\documentclass{WileyMSP-template}
\usepackage[utf8]{inputenc}
\usepackage{textgreek}
\usepackage{bbm}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{longtable}   
\usepackage{booktabs}   
\usepackage{array}  
\usepackage{booktabs}
\usepackage{geometry}
\usepackage{caption}   

\begin{document}


\pagestyle{fancy}
\rhead{\includegraphics[width=2.5cm]{vch-logo.png}}


\title{Deep Learning-Based Emotion Recognition using EEG: A Comprehensive Review}

\maketitle



% Author: Please give full first and last names for authors and include * after the name of all corresponding authors

\author{Shihab Sarar}
\author{Md Niaz Imtiaz*}
\author{Naimul Khan}


% Dedication

\dedication{}

% Affiliations: Please provide adacemic titles (Prof. or Dr.) for all authors where applicable, and include an institutional email address for all corresponding authors
\begin{affiliations}
Shihab Sarar\\
Department of Computer Science and Engineering, Ahsanullah University of Science and Technology, Love Road, Tejgaon Industrial Area, Dhaka-1208, Bangladesh\\
Email Address: shihabsararchamok@gmail.com

Md Niaz Imtiaz\\
Department of Electrical, Computer and Biomedical Engineering, Toronto Metropolitan University, 350 Victoria St, Toronto, ON M5B 2K3, Canada\\
Email Address: niaz.imtiaz@torontomu.ca

Naimul Khan\\
Department of Electrical, Computer and Biomedical Engineering, Toronto Metropolitan University, 350 Victoria St, Toronto, ON M5B 2K3, Canada\\
Email Address: n77khan@torontomu.ca

\end{affiliations}


% Keywords: Please provide a minimum of three and a maximum of seven keywords, separated by commas

\keywords{electroencephalography (EEG); emotion recognition; affective computing; deep learning; cross-subject learning; brain–computer interface (BCI); signal processing}

\textbf{Abstract}

\begin{abstract}
Emotion recognition using EEG is a growing trend in affective computing, with highly fragmented studies regarding datasets, approaches, and evaluation metrics, as well as a high degree of inter-individual variability and a rapidly changing set of deep learning approaches. In this scenario, it is difficult to extract a comprehensive view on the generalizability and practicability of the approaches considered. This work offers a structured review of emotion recognition using EEG, aiming to decrease this level of fragmentation by grouping the existing approaches following signal processing, representation learning, deep learning architectures, and cross-subject learning strategies. The most widely adopted deep learning architectures, graph-based, recurrent, and convolutional neural networks, are considered regarding their set of architectural choices, strengths, and weaknesses, as well as related trade-offs. Other major issues, overfitting, small dataset, and subject dependency, are illustrated and critically considered together with more recent approaches trying to increase the reliability of EEG emotion recognition systems. According to recent studies, the accuracy achieved in subject-specific approaches is higher than 95\%, while for the subject-independent approach, accuracy values vary from 70\% to 90\%, depending on the dataset and the architecture considered.
\end{abstract}


% Text: Please use section headings and subheadings as specified below. For communications, all section headings apart from Experimental Section should be removed
% Please make the first reference to a display item bold: \textbf{Figure 1}
% Do not abbreviate Figure, Equation, etc.; display items are always singular, i.e., Figure 1 and 2.
% Equations are always singular, i.e., Equation 1 and 2, and should be inserted using the {equation} environment, not as graphics
% Please do not use footnotes in the text, additional information can be added to the Reference list.

\section{Introduction}
Emotion recognition is at the heart of models of human behavior, since emotional states directly and profoundly affect human cognition, decision-making, and social interactions. Automatic emotion recognition facilitates the development of intelligent systems capable of adaptively responding to the affect states of users, and this is particularly critical in contemporary human-computer interaction tasks. Emotion recognition in psychology and neuroscience helps in the objective analysis of affective phenomena independently of subjective self-assessments, and in the health sector, it paves the way for numerous tasks, such as the objective evaluation of mood disturbances, patient recovery, and supportive treatment of neurodevelopmental disorders such as autism and affect dysregulation. Emotion recognition in engineering and brain-computer interface studies helps in developing adaptive systems capable of automatically responding to the affective states of users. Emotion recognition using EEG recordings is particularly useful in patient-oriented tasks in the health sector, such as patient recovery and long-term evaluation of patient moods, wherein overt behavioral manifestations of patients could be ambiguous and unavailing. EEG emotion recognition stands distinct, as it directly taps brain signals related to emotion recognition, and this makes the technology valuable in tasks where robustness and physiological validity of the detection tasks are critical.

Electroencephalography (EEG) is a non-invasive neurophysiological sensing technique that records electrical activity generated by neuronal populations with high temporal resolution. Because EEG captures brain dynamics at the millisecond level, it provides fine-grained insight into neural processes underlying emotional responses, which are often transient and rapidly evolving \cite{zheng2015eeg,koelstra2012deap}. EEG-based emotion recognition systems exploit this close coupling between brain activity and affective processing by analyzing neural responses elicited during emotional stimulation to infer underlying emotional states \cite{lin2010eegemotion}. This neurophysiological grounding distinguishes EEG from peripheral or behavioral modalities and motivates its widespread adoption in affective computing research.

EEG further provides important advantages compared to other emotion recognition methods, like facial expressions, speech, or body gestures. Behavioral cues are susceptible to intentional control, social camouflage, and context effects and are thus not very reliable in a sensitive context.  Conversely, EEG oscillations are symptomatic of inner neural activities that are hard to manipulate intentionally and supply objective estimations of affective states \cite{craik2019deep}. Although increasingly explored, at least for the future of emotion-sensitive BCIs, multimodal approaches combining EEG with other physiological or behavioral signals are not required, and EEG alone can be a robust and informative modality owing to its direct sensitivity to emotional brain dynamics and the compatibility with BCI systems.

Despite having all these benefits, there are a few challenges to recognizing emotions using EEG. The quality of an EEG signal is highly dependent on factors like electrode positioning, skin conductance, movement artifacts, and inter-session inconsistency. Variability in experimental procedures, emotional stimuli used, labeling approaches, and preprocessing techniques adds to inconsistencies \cite{alotaiby2017review}. These create difficulties in comparing methodology-based approaches systematically, especially when assessing performance on diverse databases such as DEAP, SEED, and DREAMER \cite{koelstra2012deap,katsigiannis2017dreamer}. Additionally, large variations between subjects lead to large distribution changes in the features of an EEG signal, thereby negatively affecting subject-independent generalization.

Recent breakthroughs in deep learning have led to significant improvements in EEG-based affect recognition models that are capable of learning informative features directly from raw or lightly preprocessed signals \cite{craik2019deep}. Convolutional Neural Networks (CNNs) capture channel-level relations in EEG signals \cite{li2018cnn}, Recurrent Neural Networks (RNNs) model temporal dependencies in brain activity dynamics \cite{yang2018rnn}, and Graph Neural Networks (GNNs) represent task-relevant functional connectivity between brain regions \cite{wang2022gcn}. Despite their strong ability in achieving a high level of accurate classification for subject-dependent tasks, they have been observed to have poor generalization capability for cross-subject and cross-corpus \cite{zhang2021cross}.

There has been an increased need to carry out a review of up-to-date research work in the area of emotion recognition using EEG. There are many reviews that were mainly concentrating on architectural originality or evaluation tasks on a single dataset, with less concern for cross-subject and cross-dataset generalization \cite{alotaiby2017review,craik2019deep}. Additionally, a lack of standardized preprocessing pipelines and inconsistencies in evaluation methodologies make it hard to make a valid inference \cite{peerjreview2023} on the research. There is no consensus or agreement on the best way for model design, evaluation, and implementation of effective EEG-based emotion recognition \cite{springerreview2024}.

\subsection{Contributions}
Compared with recent surveys and reviews on EEG-based emotion recognition and deep learning
this review contributes:

\begin{enumerate}
  \item \textbf{End-to-end coverage from theory to practice:}
  a unified narrative that connects emotion theory (discrete + dimensional models), neurophysiological foundations,
  stimulus/induction design, acquisition hardware evolution, multimodal affect modeling, and EEG correlates to the
  complete learning pipeline (preprocessing $\rightarrow$ feature extraction $\rightarrow$ feature selection $\rightarrow$ modeling).

  \item \textbf{A section-aligned taxonomy matching real research workflows:}
  a structured map that mirrors how EEG-ER systems are built (data/design $\rightarrow$ sensing $\rightarrow$ processing $\rightarrow$ modeling $\rightarrow$ evaluation),
  making it easier to locate open problems and best practices than method-only taxonomies.

  \item \textbf{Cross-domain feature viewpoint consolidated into one framework:}
  explicit, side-by-side synthesis of temporal, frequency, spatial, temporal--frequency, and fusion-domain feature concepts,
  including how modern deep models operationalize these representations.

  \item \textbf{Evaluation rigor emphasized beyond “accuracy”:}
  clearer separation of subject-dependent vs.\ subject-independent protocols and how they affect reported performance,
  plus a consolidated view of metrics, dataset splits, and reproducibility pitfalls.

  \item \textbf{Challenges-to-strategies linking:}
  each major challenge (redundancy, scarcity, cross-subject generalization, practical deployment constraints)
  is paired with concrete strategy families (selection/regularization, augmentation/generation, domain adaptation, lightweight pipelines).

  \item \textbf{Modern comparison lens:}
  an explicit “what is covered vs.\ missing” matrix across leading recent reviews, highlighting under-covered but important
  aspects (stimulus design, acquisition systems, multimodal modeling, and practical constraints) that this paper includes.
\end{enumerate}

\begin{table*}[htb]
\centering
\small
\renewcommand{\arraystretch}{1.1}
\setlength{\tabcolsep}{3pt}
\caption{Comparison of coverage across recent EEG emotion recognition reviews. The proposed review (This Work) provides comprehensive coverage across all major dimensions.}
\label{tab:coverage_comparison_transposed}

\resizebox{\textwidth}{!}{%
\begin{tabular}{p{0.26\textwidth} c c c c c c c}
\toprule
\textbf{Attribute} &
\textbf{Jafari 2023 \cite{jafari2023dlreview}} &
\textbf{Wang 2024 \cite{wang2024researchprogress}} &
\textbf{Ma 2024 \cite{ma2024peerjreview}} &
\textbf{Erat 2024 \cite{erat2024mta}} &
\textbf{Chen 2025 \cite{chen2025asocreview}} &
\textbf{Geng 2025 \cite{geng2025dlreview}} &
\textbf{This Work} \\
\midrule

Emotion-Theory &
 & $\checkmark$ &  &  &  &  & $\checkmark$ \\

Subject-Dependent &
 & $\checkmark$ & $\checkmark$ &  &  & $\checkmark$ & $\checkmark$ \\

Subject-Independent &
 & $\checkmark$ & $\checkmark$ &  &  & $\checkmark$ & $\checkmark$ \\

Acquisition &
 &  &  &  &  &  & $\checkmark$ \\

Signal Processing &
$\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ \\

Model Taxonomy &
$\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ \\

Evaluation \& Benchmarking &
$\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ \\

\bottomrule
\end{tabular}%
}
\end{table*}

This review integrates scattered research on EEG emotion recognition into a cohesive view. It provides an overview of the essential challenges of EEG signal processing, feature extraction, and deep learning for modeling emotions and comparisons of popular neural architectures like convolutional networks, recurrent networks, and graph networks for subject-specific and subject-independent tasks. This review provides an insight into the essential trade-offs of generalization capability, robustness, and complexity that limit the application of the current solutions. Apart from this, the potential of transfer learning and domain adaptation in handling intersubject and cross-domain variability has been discussed with both limitations and opportunities. There are several other topics that will be discussed in the review to introduce an overview of the state of the art while identifying the essential research paths for improving robust and scalable solutions for EEG emotion recognition.

\section{Theoretical and Methodological Foundations}
The theoretical background defines EEG-based emotion recognition as a framework that links measurable neurophysiological activity to computational representations of affect. It models emotional state as a structured pattern distributed across time, frequency bands, and scalp locations, and it treats learning as the problem of extracting robust features or representations from noisy, non-stationary brain signals \cite{wang2024researchprogress}. It also establishes the use of emotion label names via two-way paradigms: the discrete classification paradigm, where EEG segments are classified into distinct affective states, and the continuous modeling paradigm, where predictions of valence and arousal ratings are performed \cite{ma2024peerjreview}. These standpoints shape design decisions, evaluation schemes, and generalizations over subjects and sessions \cite{li2022transferreview}.

\subsection{Emotion Recognition}
Emotion recognition is a principal goal of affective computing in pursuing the aim of enabling computational systems that can efficaciously and adaptively respond to the emotional states of humans. Notwithstanding the increasing penetration of human-computer interaction into more and more areas of everyday life, the overall lack of awareness of user emotional states in human-computer interfaces hampers the overall efficiency of such interfaces in applications that demand user-level personalization, empathy, and commitment to user well-being, thereby pursuing the need to endow computers with the potential to identify user affective states through some form of user cues or physiological signals.

Conventional emotion recognition methods exploit behavioral features, such as facial expression, speech pattern, body language, and text messages. Though these features express more palatable aspects of human emotion, they are still amenable to control and masking in a controlled manner. In this context, combining different sources of variation with physiological measures, which are less controllable at a voluntary level, constitutes a more objective solution. Among various physiological measures, EEG represents a prominent choice because of its direct sensitivity to neural mechanisms related to processing emotions and its high resolution, enabling the monitoring of rapid dynamics of emotions \cite{ma2024peerjreview,hamzah2024heliyon}.

EEG-based emotion recognition systems model tasks that relate to EEG-stimuli pairs distributed with certain emotion categories. The changes in the emotional state cause changes in EEG rhythms and spatial activity, as well as changes that could be used to extract new information on state transitions between different emotions. According to a set of recent surveys on EEG-based emotion recognition models, the traditional pipeline of EEG emotion recognition systems includes data processing, noise removal, eigenvector transformation, and machine learning inference, with a current emphasis on end-to-end models. Traditionally, models are based on some hand-engineered features from temporal, frequency, or time-frequency worlds, whereas current models are increasingly based on deep learning models that extract hierarchical features from EEG data.

Deep learning models have characterized the emotion recognition study area because of their ability to learn complex, non-linear correlations in multichannel EEG data. Convolutional neural networks have been extensively employed to leverage the spatial information among EEG electrodes, mostly applied when processing EEG maps or image-like representations of EEG signals. Recurrent and Gated Recurrent Neural Networks have been used to model time information, and their primary purpose is to capture time-dependent information of emotion relevant to the research area. Most of the recent studies incorporated graph neural networks and leveraged the connectivity between EEG channels, which gives neurobiologically valid models for emotion recognition \cite{li2022transferreview,ma2024peerjreview}.

Despite improvements in algorithm methodology, emotion recognition accuracy is known to remain highly vulnerable to specific experimental conditions. In subject-dependent approaches, the absence of significant variability leads to excellent accuracy, while subject-independent and cross-dataset approaches remain significant challenges \cite{li2022transferreview,hamzah2024heliyon}. Variability across people and recording devices, as well as divergence across protocols for invoking emotions, causes substantial distribution divergence, thereby negatively impacting overall generalization. Thus, there is a growing trend within recent studies on applying transfer learning, domain adaptation techniques, and alignment techniques for enhanced subject and recording device independence \cite{li2022transferreview,hamzah2024heliyon}.

Emotion recognition is viewed in the literature not only as a classification problem but also as a much more complex inference problem, which is affected by neurophysiological variations, experimental conditions, and generalization. Continued progress in EEG-based emotion recognition, therefore, depends on jointly advancing acquisition protocols, representation learning, and evaluation methodologies to support reliable deployment in real-world affective systems.

\subsection{Neurophysiological Foundations of Emotional States}
Neurophysiological accounts of emotion in EEG-ER interpret emotional episodes as changes in distributed brain activity that manifest as time-varying and band-limited oscillatory patterns across channels. EEG acquisition during affective stimulation, therefore, produces signals with \emph{spatial structure across electrode locations, temporal structure due to stimulus duration and sliding-window segmentation, and frequency structure because analysis commonly separates δ, θ, α, β, and γ rhythms, each of which exhibits different responsiveness under affective conditions} \cite{wang2024researchprogress}.

Within this framing, EEG-ER pipelines either follow a staged procedure (denoising → feature extraction → classifier learning) or adopt end-to-end feature learning, and they rely on assumptions about label availability and the degree to which training and test data approximate i.i.d. conditions \cite{li2022transferreview}. When these assumptions fail—especially under cross-subject, cross-session, or cross-database settings—emotion-related neural patterns appear heterogeneous, motivating modeling strategies that explicitly target domain gaps and subject bias \cite{li2022transferreview}.
EEG-ER surveys formalize feature construction as multi-domain representation learning. Temporal features describe transitive or long-term variations of events, while spectral features are the result of band-pass filtering, Fourier transforms, and other transforms like it. Spatial features describe inter-channel relationships that correspond to the spatial organization of the cortical structure of the human brain and its variability \cite{wang2024researchprogress}. This multi-domain stance links directly to contemporary architectures (CNN/RNN/GNN/Transformer variants) that operationalize spatial-temporal-spectral fusion and to transfer-learning approaches that seek domain-invariant representations under strong inter-individual variability \cite{geng2025dlreview}.

Benchmark datasets encode these neurophysiological assumptions by pairing affective stimuli (e.g., videos/music) with EEG recordings and either discrete labels or continuous ratings, thereby defining the target emotional state space and the evaluation protocol \cite{li2022transferreview}.

\subsubsection{Discrete Emotion Representation Models}
Discrete EEG-ER models treat emotion as membership in a finite set (e.g., positive/neutral/negative; basic categories), and they implement classification as a mapping from multi-channel EEG representations to class labels. The literature consistently organizes discrete modeling around (a) feature engineering or learned representations across temporal–spectral–spatial domains and (b) classifier families ranging from SVM/KNN to deep neural architectures \cite{wang2024researchprogress}.

A core empirical regularity in discrete EEG-ER is that frequency-domain features—including PSD and differential entropy—serve as widely used affective descriptors, commonly computed per band and per channel before classification \cite{ma2024peerjreview}. Discrete pipelines, therefore, often couple bandwise features with spatial modeling to reflect the channel topology, using methods such as CSP/HDCA for spatial structure or deep models that internalize spatial priors \cite{ma2024peerjreview}. 

Additionally, deep learning reviews break down the most popular discrete EEG-ER models into single models: CNN/RNN/GNN models, attention models, combination models, and domain transfer models or models that perform DG/DPre \cite{geng2025dlreview}. According to the categories of the above review, the most popular discrete models are the following: DEAP-discrete models of CNN models \cite{tripathi2017deapcnn}, hierarchical CNN models of DEAP data that use natural spectral features like differential entropy maps \cite{li2018hcnn}. RNN/GRU/LSTM variants implement the concept of temporal dependency, such as attention-driven LSTM classifiers and spatiotemporal recurrent designs \cite{kim2020lstmatt}. Parallel convolutional–recurrent designs explicitly fuse spatial filtering with temporal recurrence in multi-channel EEG \cite{yang2018pcrnn}. GNN-based discrete EEG-ER models represent electrodes as nodes and learn adjacency structures to encode functional coupling patterns \cite{zhong2020rgnn}.

Discrete state modeling also depends on the experimental regime. Surveys separate subject-dependent and subject-independent protocols because cross-subject generalization remains harder and motivates architectural choices (e.g., graph attention, domain adversarial training) and evaluation practices \cite{ma2024peerjreview}. In this context, domain adaptation methods and adversarial learning explicitly address subject bias by aligning feature distributions across individuals \cite{geng2025dlreview}. 

The transfer-learning approaches for EEG-ER problems further solidify discrete EEG-ER as a problem of learning over gaps within domains owing to differences within the subject, session, or database. Domain transfer for knowledge can be classified along the lines of instance transfer, feature transfer, parameter transfer, and relation transfer, each dealing with the discrete classification problem through minimizing the discrepancy over domains \cite{li2022transferreview}. Methods, for instance, weight assignment as in TrAdaBoost, seek to weight the source instances to align with the target domain \cite{li2022transferreview}. Others apply feature transformation or learn subspaces to reduce distribution discrepancy and include deep learning approaches that learn transfer representations \cite{li2022transferreview}. The methods for parameter transfer encompass model fine-tuning and transductive parameter transfer, making effective use of model portability across domains \cite{li2022transferreview}. 

Discrete models also incorporate label sets that are specific to the data sets. Discrete sets of categories such as anger, fear, sadness, and happiness, among others, are commonly found in public databases, and they can also help in a comparative study \cite{li2022transferreview}.

\subsubsection{Continuous Dimensional Emotion Representation Models}
Continuous EEG-ER models treat emotion as a point in a low-dimensional affective space (most commonly valence–arousal, sometimes extended with dominance/liking/familiarity), and they implement learning as regression, ordinal prediction, or discretization of continuous ratings into quadrants/bins. Reviews emphasize that datasets explicitly differ in whether they provide continuous ratings or discrete labels, and they organize benchmark corpora accordingly \cite{wang2024researchprogress}.

From a neurophysiological perspective, continuous modeling aligns with the idea that EEG dynamics vary smoothly over time and that affective response strength differs across frequency bands and windows; thus, temporal segmentation and band decomposition are foundational operations before continuous inference \cite{wang2024researchprogress}. Continuous systems typically compute bandwise features (PSD/DE/entropy) or learn representations end-to-end, and they map these representations to dimensional ratings using neural regressors or hybrid architectures \cite{li2022transferreview}.

Continuous emotion modeling also inherits the central challenge of domain gaps. Transfer-learning surveys define cross-session transfer as addressing acquisition-time variability within the same subject and define cross-subject transfer as addressing individual differences; both scenarios directly affect dimensional prediction because rating scales must remain comparable while EEG distributions shift \cite{li2022transferreview}. Cross-database transfer further compounds these difficulties due to differences in devices, experimental conditions, and label distributions, making it the most challenging scenario for stable continuous inference \cite{li2022transferreview}.

\begin{figure}
    \centering
    \includegraphics[width=0.5\linewidth]{pictures/Valence.png}
    \caption{Valence-Arousal 2D Affective Model}
    \label{fig:valence}
\end{figure}

In practice, continuous EEG-ER systems often discretize ratings into two-class (high/low) or four-quadrant valence–arousal tasks to enable comparability with classification pipelines while preserving the underlying dimensional semantics \cite{wang2024researchprogress}. Surveyed results illustrate that some methods explicitly evaluate both binary dimensional splits and quadrant partitions on datasets such as DEAP and DREAMER, reflecting a continuum between regression-style modeling and discretized dimensional classification \cite{wang2024researchprogress}.

Deep learning reviews indicate that architectures used for continuous affect frequently mirror those used in discrete settings—CNNs for local spatiotemporal patterns, RNNs/GRUs for sequential structure, and attention mechanisms for adaptive weighting—while evaluation emphasizes stability and generalization across subjects, which remains more challenging but more deployment-relevant \cite{geng2025dlreview}. Another factor, according to survey studies, is that EEG signal processing and feature extraction are still very important, given the presence of noise in EEG signals, which require signal denoising and robust representation learning for predictive processing \cite{li2022transferreview}.

Transfer learning becomes especially relevant in continuous affect because it reduces target-domain labeling demand by transferring learned structure from related subjects/sessions/databases, but it also introduces risks of negative transfer when domain gaps are large \cite{li2022transferreview}. Consequently, feature-based deep transfer learning (e.g., discrepancy minimization and adversarial alignment) emerges as a dominant direction for improving dimensional generalization \cite{li2022transferreview}.

\subsection{Emotion Induction and Stimulus Design}
Emotion induction and stimulus design determine how affective responses are reliably evoked and temporally aligned with EEG recordings. Prior methodological surveys describe emotion induction as a prerequisite for meaningful EEG labeling, because weak or inconsistent stimuli directly degrade the validity of learned affective representations \cite{alarcao2019eegsurvey}. Paradigms involving images, music, or video clips remain popular paradigms because they ensure accurate onset-offset timing, which also ensures easy repeated measurement for every participant, helping in easy segmentation and extraction of features \cite{roy2019dleegreview}. These paradigms also involve post-trial self-evaluation tasks for mapping brain responses with subjective experience in order to reinforce the accuracy of labeling at the trial level \cite{jafari2023dlreview}.

\begin{figure}[htb]
    \centering
    \includegraphics[width=0.6\linewidth]{pictures/3D_emotion.png}
    \caption{Stimulus}
    \label{fig:stimulus}
\end{figure}

Interactive and task-driven stimuli are growing in usage as alternative paradigms because they promote enduring and contextually rooted emotion processes that passive stimuli may fail to support. However, the literature underscores that such paradigms significantly amplify inter-subject variability as well as the overall complexity of the experiments \cite{lu2021bcisurvey}. Memory recall methods based on the subject's personal memory rely on personal emotion triggers but fall prey to personal cognitive and emotion variables; therefore, their cross-subject universality is limited \cite{wan2021tleeegsurvey}. In summary, modern studies on emotion processing with the EEG directly address the issue of stimuli as a trade-off between standardization, engagement, and universality \cite{wu2020tleeegbci}.

In terms of stimulus modality, the scope of induction practice also extends towards the integration of multimodal signals (such as peripheral physiology and facial expressions) in order to check if the target affective state can be expressed uniformly across the trials, which favors temporal synchronization between subjective states and physiology targets \cite{rayatdoost2020expressionguided}. Cross-corpus evaluation also favors stimulus calibration, as differences in the content of elicitation can contribute towards the shift problem in EEG representations of affect \cite{rayatdoost2018crosscorpus}. In addition, modern learning strategies (e.g., contrastive and self-supervised representation learning) encourage elicitation designs that produce diverse yet label-consistent trials, since representation objectives benefit from repeated exposure to comparable affective episodes under varying contexts \cite{shen2022contrastive}. These developments sit naturally within the continuous affect tradition, where valence–arousal modeling formalizes emotion as graded dynamics rather than rigid categories. Finally, the broader affective-computing literature shows that robust elicitation and labeling practices generalize across biosignals (e.g., ECG as well as EEG), reinforcing the view that stimulus design functions as a system-level determinant of downstream generalization \cite{sarkar2020selfsupervised}.

\subsection{Progress in Data Acquisition Technologies}
The development of EEG acquisition equipment is the basis for the development of EEG-based emotion recognition research. The early study of affective brain activity mainly used research-grade EEG systems characterized by high-density electrode arrays, gel-based sensors, and shielded laboratory settings; these provide a high signal-to-noise ratio and also very accurate spatial resolution, which is a requirement for finding the cortical dynamics of emotions reliably. However, their high cost, time-consuming setup, and participant discomfort make it difficult to scale and be practical for real-world scenarios of emotion recognition. As emotion recognition research gradually shifted from highly controlled laboratory studies toward applied and real-life contexts, the limitations of traditional EEG hardware became obvious.

EEG acquisition hardware also plays a significant role in the data’s quality, the model's generalization ability, and the system’s feasibility for use in the real world for emotion recognition systems. Hardware factors such as EEG electrodes, channels, stability of signals, and resistance to movements also affect the reliability of the acquired feature for emotions. Emotion recognition systems trained to recognize data from research acquisition systems find it difficult to generalize and recognize signals originating from consumer acquisition systems or wireless acquisition systems for data collection. Thus, acquisition system selection is not just a design choice but now a deliberate factor in model performance between subjects, data set transferability, and feasibility for use in the real world for human-computer interaction, wearable healthcare applications, or affective computing systems.

\begin{figure}
    \centering
    \includegraphics[width=0.6\linewidth]{pictures/Human_Brain_EEG.png}
    \caption{EEG Electrodes}
    \label{fig:electrodes}
\end{figure}

Hairston et al., \cite{hairston2014usability}, in their discussion on various usability problems in conventional EEG methods used for affective computing, focus on the problems caused by the use of 'gel electrodes' and the 'lack of mobility' of the user, though these methods are sure to produce a high degree of data clarity. Debener et al. \cite{debener2012mobile} present the use of mobile EEG equipment that allows for the performance of tests while performing naturalistic procedures, showing the viability of the analysis of related EEG-based emotions. Nevertheless, the raised sensitivity to motion and environmental noise is considerable. On the other hand, wearable equipment with EEG systems, including wireless transfer technology for the continuous monitoring of patients, has also been suggested by Casson et al. in \cite{casson2010wearable}. Although the use of such equipment promotes mobility along with compliance in patients, the number of channels in wearable equipment tends to be reduced, which might influence the emotion classification accuracy. Grozea et al., in their article \cite{grozea2011bristle}, develop a type of dry electrode based on a bristle to reduce gel preparation without adversely affecting signal quality. This reduces not only the discomfort in gel preparation but also improves overall signal quality through minimal head movements or extended period recording. 
Huang et al. \cite{huang2015comb} proposed comb-shaped active dry electrodes for improved scalp coupling. As more robust design solutions are developed for dry electrode upgradation, the increased complexity and cost of such manufacturing processes hinder their acceptability. Zhao et al. \cite{zhao2019calibration} explore short-calibration EEG systems with reduced channel configurations in emotion recognition, demonstrating that affective cues can be preserved even through an optimized preprocessing approach. Nevertheless, the reduced spatial resolution limits the analysis of fine-grained patterns of emotions. To compare the performance of real-time EEG signal processing carried out with consumer-grade devices, the paper by Mullen et al. \cite{mullen2015realtime} shows the impact of domain shifts due to differences in equipment and the need for effective learning schemes. OpenBCI studies \cite{openbci2013} have shown that low-cost and open-sourced EEG data acquisition platforms are well-suited for large-scale experimentation efforts. Nevertheless, low fidelity and improper placement of electrodes are still some key barriers to achieving the goal of accurate emotional recognition.

The advancement of EEG data acquisition equipment can be seen as a good example of a trade-off balancing the quality of the data with the real-world applicability of the equipment itself. Investigational equipment maintains the ability to acquire the best quality data in terms of foundational research, but wearable-style equipment can greatly help in the scalable use of emotion recognition in real-world applications. Although the advancement in terms of the use of dry sensors, wireless data transfer, and in-data-conditioning can help in the reduction of the barriers associated with the use of the equipment itself, the process still needs the acknowledgment of the importance of the role of EEG data acquisition equipment in the emotion recognition system itself, balancing all the strategies in the process in order to achieve the most successful result in real-world use cases.

\begin{table}[t]
\centering
\caption{Association between major cerebral regions and commonly observed EEG frequency bands.}
\label{tab:brain_eeg_mapping}
\begin{tabular}{ll}
\hline
\textbf{Brain region} & \textbf{Dominant EEG frequency bands} \\
\hline
Frontal lobe & Theta (4-7 Hz), Beta (13-30 Hz), Gamma ($>$30 Hz) \\
Temporal lobe & Theta (4-7 Hz), Alpha (8-13 Hz), Gamma ($>$30 Hz) \\
Parietal lobe & Alpha (8-13 Hz), Beta (13-30 Hz) \\
Occipital lobe & Alpha (8-13 Hz; dominant), Beta (13-30 Hz) \\
Central (sensorimotor cortex) & Mu rhythm (8-13 Hz), Beta (13-30 Hz) \\
Limbic-related regions (indirect EEG influence) & Theta (4-7 Hz), Gamma ($>$30 Hz) \\
\hline
\end{tabular}
\end{table}

\begin{figure}
    \centering
    \includegraphics[width=0.7\linewidth]{pictures/Bands.png}
    \caption{Bands}
    \label{fig:bands}
\end{figure}

\subsection{Multimodal Emotion Modeling}
Multimodal emotion modeling appears as an alternative solution for the deficiencies in unimodal systems due to their susceptibility to noise and contextual variations in interpreting the emotional state \cite{soleymani2017survey}. Nevertheless, human emotional responses arise through various modalities such as facial, vocal, and physical communication, which convey valuable personal affective-related communication in addition to individual sources \cite{baltrusaitis2019multimodal}. Thus, through the process of integrating multiple sources of communication, the overall view and understanding of personal emotional state and responses remain contextual \cite{baltrusaitis2019multimodal}.

Emotion analysis in a multimodal environment merges behavioral responses such as facial, speech, text, and gesture signals with physiological signals including EEG, ECG, EMG, GSR, and respiration \cite{d2018physioaffect}. External behavioral responses mainly focus on actual emotional responses. EEG focuses on internal brain aspects of affective responses that are inaccessible for conscious experience \cite{ma2024peerjreview}. EEG helps focus these responses in a temporal manner, relating them to actual emotional responses, at the neural level. They play a significant role in responding to subtle affective responses that might not be captured by other signals. The blend of EEG with the above signals helps make these responses more universally applicable.

It is reported in the literature \cite{baltrusaitis2018multimodal} that multimodal fusion paradigms, each of which corresponds to a different level of integration of cross-modal information, such as \emph{early, intermediate, and late}.

Chen et al. \cite{chen2018featurefusion} investigate early fusion by combining features from EEG, facial, and speech modalities before classification; improved results are shown when feature normalization and synchronization are appropriately applied. However, early fusion is generally plagued by the growth of dimensionality as well as increased sensitivity to misalignment across modalities, complicating training and potentially degrading performance when features are high-dimensional or poorly synchronized. Kessous et al. \cite{kessous2017multimodal} have also applied feature-level fusion in multimodal emotion tasks, where the results show that appropriate preprocessing enhances recognition but such approaches remain challenged by noise and feature heterogeneity across channels.

Proposals for intermediate fusion methods have been developed by Shin et al., \cite{shin2017complex} in which asynchronous modality combination makes use of temporal modeling and sequence alignment. Although this method improves on handling temporal discrepancies between modalities such as EEG and other modalities, it imposes additional complexity in terms of computation. Furthermore, in a different approach based on deep contextual modeling of asynchronous modalities, Liu et al. \cite{liu2024multimodal} utilize intermediate fusion; however, this has limitations in terms of high requirements for training data and in handling long-range dependencies.

Wang et al. \cite{wang2018decisionfusion} delve into a decision-level fusion where outputs from independently trained classifiers in EEG, ECG, and GSR are combined by weighting and voting mechanisms; this imposes greater robustness and flexibility within the inclusion of modalities. The technique avoids the rigid constraints of synchronization and also allows for the easy integration or exclusion of modalities but does not explore the deep intermodal relationships and may underutilize channel-correlated information. Soleymani et al. \cite{soleymani2017survey} review decision-level fusion on physiological systems and present improved generalization and discuss how feature interactions are not fully exploited.

\begin{figure}
    \centering
    \includegraphics[width=0.75\linewidth]{pictures/Modalities.png}
    \caption{Modalities}
    \label{fig:modalities}
\end{figure}

Mittal et al. \cite{mittal2020m3er} have developed multimodal deep learning models that utilize attention mechanisms for modality contribution weightings depending upon their importance in a certain context, thus reducing the impact of unreliable modality contributions. Despite these models providing good contributions to this area by reducing the effects of unreliable contributions to a decision that is derived from a multimodal approach, they are still data-intensive optimization models that may not effectively succeed in the absence of available data. Priyasad et al. \cite{priyasad2020attention} have taken this concept a step further by developing attention-based models for multimodal fusion that have improved outcomes but are facing issues of model interpretability and increased computation.

The stability of affective patterns in multimodal datasets, such as in DEAP datasets, can improve emotion recognition according to Zheng et al., \cite{zheng2019stablepatterns}, although certain problems in bias and data modalities are still present. Similarly, Correa et al. \cite{correa2018amigos} employ AMIGOS multimodal data sets and are successful with subject-independent improvements, although large inter-subject variation in multimodal combinations remains a problem in achieving higher results in this area. From a review paper by Ma et al. \cite{ma2024peerjreview}, where certain improvements in the specified area are discussed, it’s noted that multimodal data sets are constantly more successful compared to their unimodal analogs and are working toward standardizing certain techniques in data evaluation.

Multimodal emotion modeling has been identified as a development that enhances the process of affective computing by incorporating EEG along with behavioral and physiological modalities to help computers effectively address the challenges that may come about due to the utilization of a single solution to the process. Although various fusion methods have been identified to have different advantages that may come in handy during the modeling process, they present various complexities, synchronicity considerations, or robustness during a fusion evaluation process. Multimodal models have been widely recognized for their incorporation of DL to increase adaptability to different forms of diverse modalities, which may come in handy during the decision process. Even though utilizing DL may demand extensive learning resources, various literature has been published to prove that the utilization of multimodal methods is significantly superior to the utilization of unimodal methods, as confirmed by literature focusing on various multimodal datasets such as DEAP, AMIGOS, MAHNOB-HCI, or MPED datasets. From the literature, multimodal methods have been recognized as a fundamental necessity for creating an effective EEG-based modeling approach suitable for operation in a realistic human-computer environment.

\subsection{EEG Correlates of Emotional States}
The essential basis of deriving emotion through an EEG remains rooted in the principle of recognizing the inherent rhythmic activity occurring over the human cortex as an outcome of the processing of emotion. It must also be noted here, unlike the conventional belief, the processing of an emotion is rarely indicated through the peak intensity of the EEG signal, as the expression of emotion largely remains indicated through a combination of dynamic activity occurring over a certain range of frequency bands. These, in turn, manifest as a product of the inherent neural activity underlying the emotion possessed by a particular individual \cite{geng2025dlreview}.

This view has driven emotion modeling from single-feature representations toward multiband, spatially distributed, and temporally evolving neural descriptions. The emerging evidence suggests that emotionality arises from the interaction of rhythmic modulation and interregional coordination, motivating feature extraction and representation strategies that explicitly capture frequency-specific activity, spatial asymmetries, and functional connectivity. 

Understanding the spectral and connectivity correlations for emotion is at the heart of how to develop efficient EEG-based emotion recognition systems. For example, frequency bands such as alpha have proved to have a certain level of consistency and subject stability of the scheme of emotions and therefore perform well to represent states of emotions for different subjects \cite{geng2025dlreview}. As a result, alpha bands have been deemed efficient for robust feature selection for subject-dependent and subject-independent emotion recognition systems.

Unlike alpha bands, the involvement of the beta band in emotional states, especially during heightened emotional states, is well evidenced, particularly in prefrontal regions, where a link to cognitive-emotional control parallels the involvement of arousal-related emotional regulation \cite{sun2022_db_dgcn_atffn}. In addition, delta and theta bands have been found to carry complementary information, with delta band activity related to emotional salience, while theta band connectivity is related to emotional integration/coordination \cite{zhang2023_multifreq_fc_emotion}.

More importantly, emotional states are also quantified through inter-channel correlations, where larger discriminability is observed, to obtain a holistic picture of neural interactions. Using these subspaces, finer quantification of emotional states is observed based on inter-channel correlation patterns, ensuring that a discriminative emotional state is quantified over localized band power features\cite{choong2021_corr_alpha_beta_gamma}. The time alignment effect is also observed to have an essential impact on neural components based on the emotional stimuli, reinforcing its importance in maintaining neural state changes in decision-making based on affect labeling\cite{liu2018_realtime_movie_induced}. Additionally, when basing research on interpretability, there is a need to quantify emotion based on significant patterns to avoid biases in the data set, leading to the wrong neural components \cite{qing2019_interpretable_eeg_emotion}.

Meanwhile, a comprehensive collection of research on EEG-based emotion recognition was conducted and summarized by Geng et al. \cite{geng2025dlreview}, during which the discussion focused on the characterization and importance of alpha activity when recognizing and understanding overall and general emotional dynamics and how sensitivity to emotion can benefit and possibly be enhanced through the exploration and investigation of other frequency bands, apart from the alpha band. A disadvantage and a possible drawback hidden in their discussion is the fact that these findings were summarized at a conceptual level. Sun et al. \cite{sun2022_db_dgcn_atffn} introduced the dual branch dynamic graph convolutional approach that directly addresses the beta-band activities, as well as the spatial relationships among the electrodes. This model proves that the emotional intensities, including the negative arousal, are indeed significantly detected in the modulation of the prefrontal beta signals. The process remains susceptible to cognition because the beta signals are also impacted by non-emotional cognitive activities. Zhang et al. \cite{zhang2023_multifreq_fc_emotion} proposed a multimodal functional connectivity approach that could simultaneously analyze delta, alpha, beta, and theta frequency bands. The results showed that there were significant emotion-related correlations in delta frequency bands and additional discriminative information from alpha and beta frequency bands.
In a study using the correlation-based approach for various frequency bands such as alpha, beta, and gamma frequency bands. The theta frequency band contributes mainly through the connectivity measures. The limitations of the study were the absence of explicit interaction modeling, which limited the value of the approach. Choong et al. \cite{choong2021_corr_alpha_beta_gamma} have attempted to investigate the use of correlations among alpha, beta, and gamma frequency band connectivity features and how the connectivity features under this use case have accounted for the identification of relevant emotional interactions, even when these interactions do not manifest as identifiable features across individual channels. Liu et al. \cite{liu2018_realtime_movie_induced} used movie-induced affective stimuli to retain the temporal traces of neural activities and provide time-aligned labels of affective states. While this method is effective for capturing dynamic responses related to emotions, it unfortunately depends highly on the inter-subject consistency in the perception of emotions, which may not generalize well to a truly diverse population or across different cultural contexts. Qing et al. \cite{qing2019_interpretable_eeg_emotion}, on the other hand, emphasized the aspect of model interpretability with the proposed mechanisms that connect the decision process with discernible patterns present in the signal. This enables a specific approach to validate that the underlying process utilizes neurophysiologically plausible features. However, the overall level of model interpretability is still hampered by the inherent complexity.

Collectively, this set of research supports the existence of a general neurophysiological framework for emotion states as encoded by both frequency-specific modulation, spatial organization, interregional connectivity, and even dynamics. Emotional states will be represented in the brain as an ensemble process of activity over multiple frequency bands and brain regions rather than as an expression of some global brain state. Clues provided by alpha and beta rhythms will be useful for both stability and intensity information, while salience and interregional integration will be provided by the delta and theta bands, with relational information provided by functional connectivity for differentiation among emotion states. Temporal dynamics and interpretability again increase the relative neurophysiological validity of emotion state representations to support EEG feature and modeling designs that well exceed local amplitude-based emotion recognition methods.

\subsection{EEG Signal Processing (Content to be humanized)}
The objective of EEG signal processing in emotion recognition is to transform the raw and noisy EEG signals into representations that are invariant to inter-subject and inter-session variability, while capturing the neurophysiological dynamics that are relevant to emotion. In this regard, the signal processing pipeline is equivalent to a series of conditioning steps that include: (i) suppression of non-cerebral noises, (ii) standardization of the temporal structure to facilitate learning, and (iii) definition of feature spaces that reveal the temporal, spectral, and spatial characteristics of emotional states. The signal processing pipeline is not just a pre-processing step, it is a critical component that determines the information that can be extracted and utilized by the models, and it affects the robustness, interpretability, and generalizability of the models directly.

Affective EEG is usually measured using a specific protocol in which the emotional response is dynamic over time. Therefore, the processing pipeline must accommodate the ability to represent transient responses (for example, early perceptual responses) and sustained responses (for example, sustained affective responses). As the datasets move away from the controlled environment, the pipeline focuses more on the resilience of artifacts and stability across domains, encouraging the use of structured denoising, segmentation, and representations that align with the current deep learning encoder models \cite{rahman2021eegreview}.

\subsection{Preprocessing}
The signals received through an electroencephalogram are known to be quite noisy and prone to a number of artifacts, and therefore preprocessing is an essential step for the recognition of emotions through EEG signals. The preprocessing step is concerned with the cleaning and preprocessing of the raw EEG signals to improve their quality and normalize the input signals for the subsequent steps of feature extraction and classification. The preprocessing of EEG signals is an essential step for improving the SNR of EEG signals and removing external noise and artifacts such as power line noise and physiological artifacts such as eye blinks and muscle movements for better performance of the classification model for emotion recognition. It is thus important to apply proper filtering, artifact removal, normalization, and other cleaning procedures before the feature extraction step. Moreover, the use of similar preprocessing procedures such as the use of similar filters and scaling helps to eliminate variability and errors in the comparison of signals from different domains. Below, we discuss a variety of critical preprocessing procedures and approaches used in recent studies on EEG-based emotion recognition.

\subsubsection{Filtering and Noise Reduction}

The process of filtering and noise reduction is one of the fundamental steps in the preprocessing of EEG signals, in which the raw EEG signals are cleaned of various types of non-neural artifacts while keeping the relevant neural components. The EEG signal is very susceptible to external and internal noise, and thus filtering is critical in ensuring that the final signal is indicative of the cognitive or affective processes in the brain instead of other unwanted processes.

The filtering process is of utmost importance in the context of emotion recognition using EEG signals. Emotional states in the brain have very subtle effects in the standard EEG frequency bands, including delta ($\delta$), theta ($\theta$), alpha ($\alpha$), beta ($\beta$), and gamma ($\gamma$). However, these effects can very easily get buried under unwanted effects caused by low-frequency drifts (such as skin potentials or sweating), high-frequency muscle movements (EMG), or electrical noise such as power line noise.

The noise reduction techniques that are generally used are band-pass filtering for restricting the noise band to biologically relevant frequency ranges, notch filtering for eliminating noise components present at particular frequencies such as 50 Hz or 60 Hz, and spatial filtering for reducing the effect of noise artifacts. The more complex noise reduction techniques may include complex filtering methods such as regression analysis for reducing noise artifacts and blind source separation for reducing noise components. The type of filtering used, i.e., linear filtering, Butterworth filtering, and Chebyshev filtering, is important for avoiding phase distortion of emotional responses.

Even with these techniques, it is difficult to achieve an optimal SNR. The filtering techniques are such that they can eliminate genuine low or high frequency components of brain activity, which are characteristic of emotions. It is therefore critical to properly calibrate and validate filtering techniques to optimize noise reduction while preserving relevant features of the signal.

\subsubsection{Artifact Removal}

Although filtering can remove a wide range of spectral artifacts in EEG, it is sometimes inadequate to deal with non-stationary, physiological artifacts that may be generated by eye blinks, eye movements, facial muscles, and jaw activity. These artifacts may introduce sudden and large amplitude artifacts that can severely contaminate the EEG signal, especially in frontal and temporal areas that are very important for affective processing. Hence, an additional step for the removal of such artifacts is necessary to ensure that the brain activity associated with emotions is not masked by the presence of such spurious signals.

In the case of EEG-based emotion recognition, the need for effective artifact removal is even more important because of the fine-grained nature of the emotional state indicators in the neural activity. A single blink or facial movement can introduce amplitude spikes or frequency distortions that can dominate the weaker emotional signals in the data. Moreover, because emotional stimulation often involves visual or auditory stimuli, the presence of these artifacts is more likely during experimental procedures.

The general objective of artifact removal techniques is centered on separating and reducing these non-neural components without eliminating important brain activity. Blind source separation, as well as Independent Component Analysis (ICA), may be used in separating the EEG signals into statistically independent components. Components that may contain artifacts may then be reduced in intensity or removed from the signal. Other approaches may also be employed, like regression-based correction, auxiliary channels like EOG, and machine learning algorithms that may be trained in identifying patterns of artifacts.

While these artifact removal techniques are effective in improving the clarity of the signal, there are certain limitations that may be encountered in using these techniques, such as subjectivity in recognizing certain components of the signal, eliminating important brain activity, as well as the computational costs that may be required in real-time analysis. This has also spurred certain research into developing automated systems that may be able to effectively separate artifacts from important activity, especially in emotion-related analysis where signal clarity is important.

\subsubsection{Hybrid ICA-Wavelet Techniques}

Although the effectiveness of pure methods such as ICA or wavelet transform may be extremely high for removing certain types of noise from the EEG signals, the use of combinations of such techniques may prove to be even more effective. Recently, the use of hybrid models of ICA and wavelet transform techniques has been proposed as an effective tool for the cleaning of EEG signals. The use of such techniques may prove to be extremely effective for the cleaning of EEG signals, especially in the context of emotion recognition, where the neural patterns are extremely subtle and are highly susceptible to being affected by various types of noise such as those due to eye blinks, muscle movements, etc. The use of such techniques may help in the cleaning of the EEG signals without affecting the emotional content.

In the area of emotion recognition, where the patterns of neural activity associated with emotional states are delicate and vulnerable to distortion by eye blinks, muscle activities, and external noise, these hybrid models can be particularly useful. These models open up the possibility of removing artifacts without damaging the emotional information conveyed by neural activity. As EEG-based affective computing systems become more sensitive and widespread, the need for automatically filtering physiological noise becomes more pressing.

Generally, hybrid ICA-Wavelet approaches begin by carrying out a statistically independent component analysis of the EEG data, which helps in the separation of the neural and non-neural components. Instead of completely removing the independent components identified as noisy, as is the case in ICA, the hybrid ICA-Wavelet approach uses wavelet analysis for the separation and elimination of the noisy components. This ensures that the final clean data include only the required components of the brain activity. This type of analysis, which considers space as well as time, is significant in dealing with the complexity of real-world data.

Although these hybrid ICA-wavelet methods are promising, certain trade-offs are also involved in their implementation, which may limit their performance depending on the type and amount of artifacts in the data, as well as the type of wavelet functions and levels of decomposition that are used in the process. Nevertheless, the impact that these hybrid methods have shown in improving classification performance in EEG emotion recognition systems shows their potential as an advanced preprocessing technique.

\subsubsection{Channel Selection and Spatial Filtering}

Channel selection and spatial filtering form part of the basic preprocessing mechanisms that focus on improving signal quality by optimizing the spatial representation of the EEG signal. Rather than considering all channels of the signal, these mechanisms focus on the most important parts of the scalp while avoiding the effects of noisy and irrelevant channels. This becomes particularly important when dealing with emotion recognition via EEG signals, where emotion signals tend to be localized and easily obscured by irrelevant neural and physiological activities.

In affective computing, it has been found that the selection of important regions of the brain, particularly those that exhibit emotion, such as the prefrontal and temporal regions, can significantly improve emotion classification. By selecting a specific subset of the electrodes, it becomes possible to reduce dimensionality, eliminate noisy channels, and simplify the process. Spatial filtering improves the clarity of the signal by re-referencing the EEG signal and eliminating global noise components, thus enhancing the signal corresponding to localized neural patterns of emotion.

Several strategies are used in this context. For instance, manual or data-driven channel selection methods are used for prioritizing certain electrodes based on neuroscientific knowledge or feature contribution metrics. This ensures that only discriminative channels are used in the process. Spatial filters like CAR and Laplacian filtering are used for reducing reference bias and eliminating common-mode noise. These filters are used for normalizing the EEG inputs for different channels and for highlighting spatial gradients that are related to emotional processes.

While these strategies are useful in several ways in terms of reducing computational requirements and improving robustness, they are not without their limitations. For instance, too much reduction of channels in this context would lead to the loss of emotional signals that are spatially distributed. Spatial filters would lead to signal topography distortion if not used properly.


\renewcommand{\arraystretch}{1.2}
\setlength{\tabcolsep}{5pt}
\begin{small}
\begin{longtable}{
>{\raggedright\arraybackslash}p{0.13\textwidth}
>{\raggedright\arraybackslash}p{0.18\textwidth}
>{\raggedright\arraybackslash}p{0.14\textwidth}
>{\raggedright\arraybackslash}p{0.18\textwidth}
>{\raggedright\arraybackslash}p{0.18\textwidth}
>{\raggedright\arraybackslash}p{0.19\textwidth}
}
\caption{Representative studies on EEG preprocessing techniques for emotion recognition.}
\label{tab:eeg_preprocessing_summary} \\

\toprule
\textbf{Study} & \textbf{Approach} & \textbf{Preprocessing Method} & \textbf{Advantages} & \textbf{Limitations} & \textbf{Key Findings} \\
\midrule
\endfirsthead

\multicolumn{6}{c}{\textit{Table~\ref{tab:eeg_preprocessing_summary} continued}} \\
\toprule
\textbf{Study} & \textbf{Approach} & \textbf{Preprocessing Method} & \textbf{Advantages} & \textbf{Limitations} & \textbf{Key Findings} \\
\midrule
\endhead

\midrule
\multicolumn{6}{r}{\textit{Continued on next page}} \\
\endfoot

\bottomrule
\endlastfoot

Somers \textit{et al.}~\cite{somers2018mcwfilter} & Multi-channel Wiener filtering for artifact suppression & Artifact Removal & Preserves neural signal while reducing ocular/muscle noise & Needs covariance estimates; less effective for nonstationary noise & Improves EEG signal quality before emotion decoding \\

Kang \textit{et al.}~\cite{kang2019_ica_aug} & ICA with heuristic artifact-aware rejection & Artifact Removal & Effectively isolates and removes major EEG artifacts & Manual component selection may reduce reproducibility & Boosts classification accuracy post-cleaning \\

Phadikar \textit{et al.}~\cite{sphadi2021automati} & Wavelet thresholding for blink artifact removal & Artifact Removal & Fast and adaptive for blink noise & Limited to blink artifacts; not general-purpose & Strengthens EEG robustness in emotion pipelines \\

Koelstra \textit{et al.}~\cite{Koelstra2012} & Bandpass filtering with baseline normalization & Channel Selection and Spatial Filtering & Reduces noise and inter-subject variability & Requires baseline segment; may remove useful bands & Improves single-trial emotion classification \\

Mahajan \& Morshed~\cite{Mahajan2014} & Wavelet-enhanced ICA artifact removal & Hybrid ICA-Wavelet Techniques & Automated ocular artifact suppression & Mainly targets eye-blink artifacts & Enhances EEG quality for emotion analysis \\

Val-Calvo \textit{et al.}~\cite{ValCalvo2019} & Lightweight real-time preprocessing pipeline & Channel Selection and Spatial Filtering & Low-latency and computationally efficient & Simplified features limit generalization & Enables real-time EEG emotion recognition \\

Dura \& Wosiak~\cite{dura2021channelstrategy} & Channel selection strategy to reduce electrodes for deep learning emotion ER & Channel Selection and Spatial Filtering & Reduces channels while retaining discriminative information; lowers computation & Strategy may be dataset/model-dependent; risk of losing spatial cues & Shows that careful channel reduction can maintain emotion classification performance \\

Mammone \& Morabito~\cite{mammone2014awica} & Enhanced AWICA (wavelet-assisted ICA) for automatic artifact suppression & Hybrid ICA-Wavelet Techniques & Automated removal with reduced manual intervention; effective on nonstationary artifacts & Performance depends on decomposition settings; may distort low-amplitude neural components & Improves EEG cleanliness via wavelet-based component processing before downstream analysis \\

Kumar \textit{et al.}~\cite{kumar2019wica} & Wavelet-enhanced ICA for ocular artifact removal in EEG & Hybrid ICA-Wavelet Techniques & Better EOG suppression than plain ICA; preserves more neural content using wavelet denoising & Computationally heavier than simple filtering; parameter tuning required & Demonstrates improved ocular artifact attenuation with wavelet+ICA hybrid processing \\

\end{longtable}
\end{small}


The preprocessing of EEG data is the critical first stage that makes up the entire frame of reference and determines whether brain patterns that correspond to emotional states can be simultaneously detected. The drive for affective EEG signatures to be consistently identified relies on a number of factors such as; their low amplitude nature; the amount of ocular, muscular, and nonstationary artifacts present; etc., and as a result, preprocessing conditions, which can either improve or diminish classification accuracy, generalizability, and reproducibility. 

The studies presented in the table can be grouped into four complementary categories. Artifact removal strategies, including multi-channel Wiener filtering, independent component analysis (ICA) based rejection, and wavelet thresholding have been shown to consistently improve the overall quality of the signal, and also have an impact on downstream recognition, but their applicability involves trade-offs such as the validity of the covariance estimated like Gaussian, the subjectivity associated with selecting the appropriate components, or the location of the artifacts being targeted. Hybrid ICA-wavelet approaches attempt to merge the separation component of ICA with the time-frequency denoising capability of wavelets in order to automate the suppression of ocular artifacts, though the benefits of such techniques may be skewed towards blinks and need to be tested on other sources of artifacts. Channel selection and spatial filtering strategies like using baseline normalized bandpass filtering, help to mitigate noise and inter subject variability, but rely heavily on the availability of baseline data, and may also attenuate informative bands unintentionally. Lightweight real-time pipelines are emphasized to provide low latency and computationally efficient online affective interfaces but not all of the simplifications used in these pipelines are designed to ensure that robustness will be obtained across subjects.

It can be inferred from the Table \ref{tab:eeg_preprocessing_summary} that preprocessing must be performed according to deployment constraints: aggressive data cleaning is needed when conducting offline modeling; standardized methods should be used in order to provide comparability between preprocessing results; and streamlined processing must occur when carrying out inference in real time.

\subsubsection{Feature Extraction}
The process of feature extraction transforms the preprocessed EEG signals into a form where emotion-related features become more easily separable. The most important assumption is that affective neural responses occur collectively in time, frequency, and scalp space. Hence, features relevant to emotions cannot be found in a single domain. Feature extraction techniques can generally be divided into two classes: (i) explicit representations, where features can be derived from signals, and (ii) learned features, where deep models learn features from raw or minimally preprocessed EEG or engineered features.

The most important trend in EEG-based emotion recognition is the development of structured, model-friendly tensor representations that keep track of inter-domain dependencies. Thus, rather than obtaining separate features for each channel, as in traditional feature extraction, recent pipelines often compute 2D or 3D objects, such as channel $\times$ time, channel $\times$ frequency, or time $\times$ frequency maps per channel, so that CNNs can take advantage of local features. Another trend is based on 3D feature representations along with fully convolutional models, where temporal, spatial, and spectral information is treated as a unified entity, eliminating the need for inter-domain fusion and enabling end-to-end learning of inter-domain interactions \cite{li2021_3dfeatdfcn}.

Sequential feature construction is also common due to the temporal nature of emotional responses.Sequential feature construction is also very common due to the nature of emotional responses. Spatial-temporal features can be constructed by stacking window-level features and using sequence models like bidirectional recurrent units to model temporal dependencies among windows. This approach maintains local temporal information while also modeling long-term affective dynamics, which is important when the emotional episodes develop gradually rather than suddenly \cite{lew2020bigruspatiotemporal}.

Moreover, transformer-aided feature construction can facilitate cross-domain features, as it does in attention-driven fusion. Architectures based on conformer, which integrates convolution for local pattern detection and transformer attention for global interdependency detection, can provide a useful tool for constructing features as local patterns or global connections without resorting to predefined fusion strategies \cite{song2022eegconformer}. Additionally, the parallel use of transformers and 3D convolution is also seen to be following the same principle, where the role of the transformer is to identify interdependencies, while the role of 3D convolution is to identify structurally well-defined spatiotemporal relationships that are associated with Emotion concepts \cite{sun2022paralleltransformer3dcnn}.


In the majority of emotion recognition systems based on the EEG, the frequency domain features are used, which are derived from the preprocessed signals. Some of the commonly used features, such as power spectral density (PSD) and differential entropy (DE), are widely used to represent the EEG signals in emotion recognition systems. The power spectral density features represent the power of the signal in standard bands such as theta, alpha, beta, etc., which indicates the amount of rhythmic activity present in the signal in the respective bands. The concept of differential entropy is related to power spectral density, but when we consider a Gaussian distribution of the EEG signal amplitude in a short period of time, the differential entropy $h$ can be expressed as
\begin{equation}
    h = \frac{1}{2}\ln(2\pi e,\sigma^2), 
\end{equation}
where $\sigma^2$ is the variance of the signal. Note that variance is proportional to signal power in the window, and thus DE is a log-power feature. In effect, calculating DE on an EEG epoch will give a value that is proportional to the signal power in the epoch and can be calculated on a band-by-band basis so, DE for delta, theta, etc. can be calculated in a manner similar to calculating band power.

The power spectral density (PSD) and differential entropy (DE) are popularly used in EEG-based emotion recognition systems, as they are effective in capturing the characteristics of brain signals related to emotions in the frequency domain. Out of these, DE is regarded as a better alternative to power-based features, owing to its numerical stability and normal distribution. The spectral features, in general, tend to be extracted from various bands of brain signals, including delta, theta, alpha, beta, and gamma bands. These features offer a compact representation of signals, which is of a fixed length and can be readily incorporated with ML. Among the advantages that make PSD or DE features noteworthy is that these features remain unaffected by any timing differences that might be present in a trial. In addition, these features, once appropriately filtered, tend to be somewhat immune to high-frequency noise. Yet, by averaging the power over a time window, these features might overlook significant information regarding the dynamics that occur over a more limited period. Additionally, the stationarity of the signal over a given period might be a concern if the emotional changes do not occur uniformly over the course of the stimulus presentation. Ultimately, with regard to these issues, the parameters for the window size, overlap, and frequency resolution must be adjusted properly. While these measures have limitations, the PSD and the DE remain core components in the study of affective EEGs, often providing a foundation for more intricate models.

In summary, the preprocessing step is an essential constituent of the EEG-based emotion recognition approach, which has a direct bearing on the reliability of the results. As can be observed across the literature, there has been an increasing trend towards the development of sophisticated and automatic preprocessing methodologies. For instance, the initial research studies employed basic filtering and manual removal of artifacts, but the latest research studies, including the ones discussed above, have employed sophisticated methods like ICA cleaning, wavelet denoising, and automatic channel selection to clean the EEG signal. The general idea is that the better the signal quality, the better the model will perform. Another trend observed across the literature is the standardization of the preprocessing methodologies. For example, the data of different subjects or sessions is often passed through the same filtering and feature extraction processes. As the quantity and quality of EEG emotion data increase, and the need for online processing of the data for real-world applications emerges, the preprocessing techniques are shifting towards the development of real-time capable preprocessing methods, reducing the dependency on manual intervention during the process. In the future, the preprocessing techniques are expected to continue the progression towards the inclusion of newly emerging signal processing techniques for the improvement of the quality of the EEG data, thus providing a solid base for the accurate and robust recognition of emotions using brain signals, as the best classifier can only perform as well as the data it is provided with.

\paragraph{Temporal, Frequency, and Spatial Feature Concepts \& Cross-Domain Feature Fusion}
The nature of electroencephalogram (EEG) data is inherently multi-domain: temporal, frequency, and spatial. Each of these domains represents different aspects of brain activity. The temporal domain represents the evolution of brain activity over time, which can be achieved by dividing the EEG signal into time intervals or by employing time series analysis (using windowing techniques). Features in the frequency domain are achieved by transforming the EEG signal (using Fourier transform) and analyzing the signal's power in specific frequency bands: δ (1-3 Hz), θ (4-7 Hz), α (8-13 Hz), β (14-30 Hz), and γ (30-50 Hz). Note that different frequency bands are associated with different levels of emotional content: previous studies have shown that specific frequency bands (beta, gamma) are more active during specific emotional content. One of the frequency domain features is differential entropy (DE), which is defined as
\begin{equation}
H(X) = \frac{1}{2}\ln\left(2\pi e\,\sigma_X^2\right)
\end{equation}
where $\sigma_X^2$ is the variance of the signal in the band. Features from multiple bands of the DE provide a compact representation of the EEG signal spectrum, which is useful in emotion recognition. The spatial features aim to capture the distribution of the EEG signal in the brain. For example, it is possible to compute the inter-channel correlation matrix $R$, which can be defined such that the $(i,j)^{th}$ element is the Pearson correlation $r_{ij}$ between the $i^{th}$ and $j^{th}$ channels,
\begin{equation}
r_{ij} = \frac{\operatorname{Cov}(x_i, x_j)}{\sqrt{\operatorname{Var}(x_i)\operatorname{Var}(x_j)}}
\end{equation}
which is the adjacency matrix for the graph representation of the spatial features. Thus, the EEG signal provides multi-domain features: the dynamics of the signal over time, the spectral features of the signal, and the spatial distribution of the signal over the cortex. These are called multi-domain features, and the use of all three can provide a holistic view of the emotional state.

\begin{figure}
    \centering
    \includegraphics[width=0.75\linewidth]{pictures/tem_freq_spa.png}
    \caption{Temporal vs Frequency vs Spatial Domain}
    \label{fig:temp_freq_spatial}
\end{figure}

Each of these features provides supplementary information for emotion recognition from EEG, and their combination will result in stronger and more accurate emotion recognition models. Temporal features reflect the dynamic nature of emotional responses and their variation. Emotions are processes that depend on time. For example, emotional arousal increases and then decreases over time. Frequency features focus on specific frequency bands of neurophysiological responses that are related to emotional processes. For example, alpha or beta band power increases reflect relaxation or higher engagement. Spatial features reflect emotional processes in specific brain regions. For example, frontal alpha asymmetry is known to reflect affective valence. This is because these three aspects are complementary, and hence their combination can compensate for the limitations of any of these individual feature types. In fact, empirical studies have demonstrated that models that use combined temporal, spectral, and spatial representations outperform those that use individual types of these features. In particular, spatial features are often combined with temporal or frequency features to provide context about where in the brain particular types of temporal or spectral patterns are present. The combination of multiple types of features from multiple domains, therefore, creates a more comprehensive representation of the EEG signal, which can potentially improve accuracy and robustness of emotion recognition against noise or variation.

Fusion is achieved using various architectural styles. First, \emph{tensor fusion} involves the creation of multi-dimensional input representations (e.g., 3D tensors) that enable an encoder network to learn interaction patterns implicitly. Second, \emph{multi-branch fusion} involves the use of dedicated encoders for each domain (temporal, spectral, and spatial) and subsequent fusion of intermediate representations. Third, \emph{attention-based fusion} leverages adaptive weightings that highlight the most relevant domain or channel representations depending on varying noise conditions. The use of a dual-branch dynamic graph convolution and adaptive transformer fusion presents an example of the potential for using spatial reasoning and attention-based fusion mechanisms that leverage inter-channel relationships and remain adaptive in feature weightings \cite{sun2022atffnet}. For multi-domain EEG encodings, transformer networks are also developed that take in representations from various feature spaces and learn to fuse these using self-attention \cite{sun2023meegtransformer}.

\subparagraph{Temporal Domain Features:}
Temporal domain features play a critical role in emotion recognition systems that use EEG as a medium, as emotions are dynamic experiences that change over time. Emotions are dynamic experiences that change over time and are often composed of a series of neural events that can't be described by time-independent features. Temporal features help the emotion recognition system understand how brain activity is changing over time as a response to an emotional stimulus. These features are critical in handling intersubject variability in the timing of the emotion recognition response, as they help the system concentrate on the timing of the signal rather than relying on synchronous stimuli and labels.

Within the emotion decoding paradigm, temporal modeling has been integrated using a variety of deep learning approaches that have taken advantage of the strengths of sequence learning. As an example, recurrent models such as LSTMs or bidirectional RNNs have been widely used to capture the dependencies between successive segments of the EEG signals, modeling the temporal dynamics of affective states. In other instances, hybrid models have been proposed that take advantage of the strengths of both convolutional and recurrent models to learn the spatial patterns and their temporal dynamics. In addition, hierarchical sequence models have been proposed that learn the signals regionally and then aggregate them globally, thus preserving the spatial patterns while modeling the temporal dynamics. Finally, temporal convolutional models have been proposed that offer an alternative to recurrent models, enabling the modeling of long-range dependencies using hierarchical, dilated convolutional architectures.

Although these methods are useful in terms of the recognition of dynamic patterns of emotion, there are challenges that are associated with these methods. These challenges include difficulties in integrating frequency and spatial information or the use of uniform timing. The selection of appropriate window length and temporal resolution is also important in temporal domain modeling. This is important for the avoidance of overfitting or the loss of relevant information, particularly for the improvement of the performance of emotion recognition, especially for naturalistic or continuous emotional scenarios where there is the presence of individual variations of emotional expression timing.

\subparagraph{Frequency Domain Features:}
The features identified by the frequency domain have proven to be a promising approach to study the rhythmicity of the EEG signals, which are closely related to the emotional processes of the human brain. These features have enabled researchers to study the power distribution of the EEG signals over standard frequency bands such as alpha, beta, theta, or gamma waves, which are known to be influenced by emotional processes. Unlike features in the time domain, features in the frequency domain offer a more consistent representation of rhythmic activity in the brain, which is known to endure over sessions and subjects, especially if the EEG signals are appropriately filtered. 

Features in the frequency domain are critically important in emotion recognition models, as these offer a means to explore the power in EEG signals in certain bands that is linked to emotional processes, as supported by neurophysiological studies. For example, increased power in the beta frequency band in frontal areas of the brain is known to be related to increased emotional arousal, while power asymmetries in the alpha frequency band are related to valence changes. These features are not easily influenced by timing jitter or noise in EEG signals, which makes them appropriate for real-world EEG-based emotion recognition models.

Several methodologies have been explored for the effective modeling of frequency domain features. For instance, in some methodologies, the EEG signals were converted into spectrograms using various tools such as the STFT, allowing deep learning models to effectively learn the relative contribution of frequency domain features~\cite{zheng2022multi}. In other methodologies, the band power and differential entropy were considered as feature inputs, allowing the model to effectively capture the spectral content of each channel. More recent methodologies have also been explored, allowing the model to effectively capture the spectral relationship between channels using tools such as the convolutional model and attention mechanism~\cite{song2022eegconformer}. In addition, multi-domain models have also been explored, allowing the model to integrate frequency domain and temporal domain representations for improved emotion recognition accuracy~\cite{li2021multidomain}.

However, frequency domain modeling for emotion detection with its emphasis on consistent rhythmic patterns has its own set of limitations. For example, there is a need for balance in terms of spectral resolution and computational efficiency. In addition, the parameters of transformations such as STFT are critical and may affect the overall model. Moreover, frequency domain modeling may not account for other dynamics that are critical in decoding complex emotion responses.

In summary, frequency domain features form an essential pillar in EEG-based emotion recognition systems. They are reliable and highly correlated with other emotion-related features. In addition, frequency domain modeling with other domains forms an essential tool in modeling complex affective states with neurophysiological meaning~\cite{li2021multidomain, song2022eegconformer, zheng2022multi}.


\subparagraph{Temporal-Frequency Domain Features:}
Frequency domain features are the core of emotion recognition using EEG, as they are based on the rhythmic patterns of the brain's electrical activity, which are directly related to emotional states. The rhythmic patterns of the electrical signals in certain bands of frequencies, such as the delta band (1-4 Hz), the theta band (4-8 Hz), the alpha band (8-13 Hz), the beta band (13-30 Hz), and the gamma band (30-50+ Hz), are known to relate to various aspects of emotion. For instance, the power of alpha and beta rhythms is known to relate to the level of arousal and the valence of emotion. The asymmetry of alpha power in the frontal region is known to relate to approach/withdrawal aspects of emotion. Frequency-specific modulations are generally consistent across subjects and sessions, and hence are useful indicators of emotion classification.

In practice, the analysis of the electrical signals in the frequency domain is achieved through various techniques such as the Fourier transform, the wavelet transform, and the differential entropy method. The above techniques are used to transform the raw electrical signals to a more compact representation. The compact representation is useful because the electrical signals are more susceptible to noise.

Recently, various modeling techniques have adopted frequency domain features in different ways. In some models, frequency domain feature extraction has been performed independently, and the extracted features have been combined with temporal or spatial features to obtain enhanced emotion-specific information. In other models, convolutional or attention-based encoders have been employed to extract frequency domain features, which have been treated similarly to visual inputs to learn abstract frequency domain representations. In addition, various models have utilized graph-based reasoning to extract frequency domain feature-encoded nodes to identify inter-band relationships and regional patterns.

Even though frequency domain-based models have shown promising performance, various trade-offs have been identified in their performance, including sensitivity to the parameters of the decomposition and windowing techniques employed in the models. In addition, models that have adopted frequency domain-based features may overlook essential temporal patterns, thus failing to accommodate different subjects with varying frequency domain patterns. Nonetheless, frequency domain-based features have shown promising performance when combined with other features to provide robust brain state oscillatory patterns and improve emotion recognition systems.

\subparagraph{Integration and Fusion Strategies Across Feature Domains:}
The integration of temporal, frequency, and spatial features has emerged as a primary concern in EEG-based emotion recognition, owing to the multidimensional nature of emotional brain responses. These features describe emotional responses from different but complementary perspectives, where temporal features describe the dynamics of brain responses, frequency features describe brain responses in terms of oscillatory properties, and spatial features describe brain responses in terms of brain regions. Emotion expression in the brain does not occur in individual channels or frequency bands but occurs through interactions across multiple brain responses. Therefore, brain responses that combine multiple views are more expressive and tend to generalize better across subjects and sessions.

The importance of multi-domain fusion becomes critical when handling noisy or uncertain EEG signals. There are scenarios where one domain may not contribute much or may be noisy, such as noisy temporal responses owing to movement, but brain responses in other domains can serve as a fallback, ensuring that fusion-based methods are more robust, especially when handling real-world or cross-subject scenarios.

However, recent developments in multi-domain fusion architectures have focused on flexibility and adaptability. Many of these methods involve multi-branch architectures in which domain-specific feature extractors process temporal, spectral, and spatial information in parallel, followed by fusion layers that combine the features. Some of these methods involve graph convolutional networks (GCNs) to represent spatial relationships between EEG channels, combined with temporal-frequency encoders. Others involve attention mechanisms to dynamically assign weights to features across domains according to their relevance to the emotional state.

There is also an emerging trend towards deeper architectures that learn their own fusion policies implicitly. These include transformer-based feature encoders with domain-specific attention mechanisms and approaches with masked modeling for cross-domain reconstruction. These are very powerful architectures. However, they tend to be computationally expensive, requiring significant memory and regularization during training to avoid overfitting. Moreover, they also present difficulties with regards to their interpretation, as it is not clear what contribution each of the different domains makes towards the overall emotion prediction.

To recapitulate, the integration of various EEG domains can improve the model’s ability to learn discriminative and generalizable representations of emotions. Fusion techniques can provide better modeling of emotional patterns, handling of noise, and stability of cross-subject models. Although the above techniques provide considerable benefits, the trade-off needs to be managed for the handling of computational complexities and the interpretability of the models. It is believed that future research will concentrate on the development of more lightweight, interpretable, and data-efficient fusion techniques that can provide the benefits of multi-domain integration.applications.

\renewcommand{\arraystretch}{1.2}
\setlength{\tabcolsep}{5pt}
\begin{small}
\begin{longtable}{
>{\raggedright\arraybackslash}p{0.11\textwidth}
>{\raggedright\arraybackslash}p{0.20\textwidth}
>{\raggedright\arraybackslash}p{0.15\textwidth}
>{\raggedright\arraybackslash}p{0.16\textwidth}
>{\raggedright\arraybackslash}p{0.18\textwidth}
>{\raggedright\arraybackslash}p{0.20\textwidth}
}
\caption{Representative studies on EEG feature extraction techniques for emotion recognition.}
\label{tab:eeg_feature_extraction_summary} \\

\toprule
\textbf{Study} & \textbf{Approach} & \textbf{Domain Category} & \textbf{Advantages} & \textbf{Limitations} & \textbf{Key Findings} \\
\midrule
\endfirsthead

\multicolumn{6}{c}{\textit{Table~\ref{tab:eeg_feature_extraction_summary} continued}} \\
\toprule
\textbf{Study} & \textbf{Approach} & \textbf{Domain Category} & \textbf{Advantages} & \textbf{Limitations} & \textbf{Key Findings} \\
\midrule
\endhead

\midrule
\multicolumn{6}{r}{\textit{Continued on next page}} \\
\endfoot

\bottomrule
\endlastfoot

Li \textit{et al.}~\cite{li2021_3dfeatdfcn} & 3D spatial-temporal-spectral features with FCNs & Spatial-Temporal-Spectral & Combines rich domain information in unified volume & Heavier computation than 2D encodings & Improves accuracy across datasets \\

Lew \textit{et al.}~\cite{lew2020bigruspatiotemporal} & Spatiotemporal encoding with Bi-GRU & Spatial-Temporal & Captures evolving emotion responses & Sensitive to segmentation granularity & Better for gradual emotion transitions \\

Song \textit{et al.}~\cite{song2022eegconformer} & Conformer model for EEG sequences & Spatial-Temporal & Fuses local and global patterns effectively & Can overfit without regularization & Outperforms traditional CNN/RNN models \\

Jia \textit{et al.}~\cite{jia2020sstemotionnet} & 3D DenseNet with attention across domains & Spatial-Temporal-Spectral & Learns dynamic relevance between features & Model complexity is high & Salient EEG areas enhance emotion decoding \\

Sun \textit{et al.}~\cite{sun2022paralleltransformer3dcnn} & Parallel transformer + 3D CNN fusion & Spatial-Temporal & Separates local/global representation branches & Careful tuning required for balance & Shows strong performance on multi-channel inputs \\

Yudhana \textit{et al.}~\cite{yudhana2020human} & FFT-based spectral features with $k$NN classifier & Spectral & Simple and computationally efficient & Ignores temporal dynamics; limited scalability & Shows the viability of classic frequency features \\

Chen \textit{et al.}~\cite{chen2019feature} & Differential entropy features with LDA projection & Spectral & Compact and discriminative spectral representation & Linear assumptions limit expressiveness & Improves valence and arousal classification \\

Bigirimana \textit{et al.}~\cite{bigirimana2020emotion} & Multi-class CSP spatial filtering & Spatial & Enhances emotion-specific spatial patterns & Sensitive to non-stationarity and subject variation & Boosts multi-class EEG emotion accuracy \\

Lin \textit{et al.}~\cite{lin2010eegpower} & Power spectral asymmetry analysis for emotion classification & Frequency & Captures hemispheric imbalance related to valence & Sensitive to noise and baseline drift & Demonstrates frontal alpha asymmetry correlates with emotional valence \\

Jenke \textit{et al.}~\cite{jenke2014feature} & Comparative evaluation of frequency-domain EEG features & Frequency & Identifies robust spectral descriptors for emotion decoding & Requires feature selection optimization & Highlights differential entropy and PSD as strong frequency predictors \\

Al-Nafjan \textit{et al.}~\cite{alnafjan2017psd} & PSD-based spectral feature extraction with SVM & Frequency & Simple and computationally efficient & Limited temporal resolution & Confirms spectral power variations distinguish valence states \\


Murugappan \textit{et al.}~\cite{murugappan2010time} & Statistical time-domain features for emotion classification & Temporal & Low computational cost; suitable for real-time systems & Limited ability to capture nonstationary dynamics & Shows time-domain variance and mean features discriminate emotions \\

Gupta \textit{et al.}~\cite{gupta2019temporal} & Hjorth parameters and higher-order statistics & Temporal & Reflects signal activity and mobility characteristics & Sensitive to artifacts & Demonstrates temporal descriptors contribute to valence-arousal recognition \\

Petrantonakis \& Hadjileontiadis~\cite{petrantonakis2010emotion} & Higher-order crossings (HOC) for EEG temporal dynamics & Temporal & Captures nonlinear signal characteristics & Requires careful parameter tuning & HOC features enhance multi-class emotion accuracy \\


Zhao \textit{et al.}~\cite{zhao2019stft} & Short-Time Fourier Transform for emotional EEG analysis & Temporal-Frequency & Preserves both temporal and spectral information & Window-size selection impacts performance & STFT-based representations improve arousal classification \\

Subasi \& Gursoy~\cite{subasi2010wavelet} & Wavelet transform for EEG emotional state detection & Temporal-Frequency & Handles non-stationary EEG signals effectively & Higher computational complexity & Wavelet coefficients improve emotional discrimination \\

Huang \textit{et al.}~\cite{huang2019hilbert} & Hilbert-Huang transform for nonlinear EEG analysis & Temporal-Frequency & Adaptive decomposition for nonlinear signals & Mode mixing may occur & Enhances representation of emotion-related oscillatory patterns \\


Li \textit{et al.}~\cite{li2018fusion} & Feature-level fusion of spatial and spectral EEG descriptors & Fusion & Integrates complementary information & Risk of high dimensionality & Fusion improves robustness across subjects \\

Zheng \& Lu~\cite{zheng2015investigating} & Combined frequency and spatial asymmetry features & Fusion & Exploits hemispheric asymmetry and band power & Requires careful channel selection & Identifies critical bands and regions for emotion decoding \\

Zhang \textit{et al.}~\cite{zhang2020multifeature} & Multi-feature fusion with deep neural networks & Fusion & Learns hierarchical representation from heterogeneous features & High model complexity & Feature fusion enhances cross-subject generalization \\

\end{longtable}
\end{small}

The table~\ref{tab:eeg_feature_extraction_summary} presents an overview of the various EEG feature extraction methods that have been used for recognizing emotional states. In particular, it categorizes the different feature extraction methods or paradigms according to the type of signals used to construct them: either based on the spectral or spatial domains. The predominant early and light feature extraction methods were primarily based on spectral representation of the signals through the use of the Fourier transform or differential entropy. On the other hand, the predominant features extraction paradigms in the spatial domain were based on filtering techniques that utilized common spatial patterns as the basis for classification.

There is increasing use of these hybrid spatial-temporal-spectral representations in current research, due to their ability to accurately capture the complex and nonlinear characteristics of emotions associated with brain activity. Methods that use three-dimensional feature volume representations, attention-based methods, and deep learning architectures (convolutional or transformer-based) all combine multi-modal data across domains to produce better, stronger and more precise emotion decoding results. Spatiotemporal sequence modeling methods, such as Bi-GRU and conformer based models use time to encode how emotions are thought to change continuously over time while maintaining the spatial aspect, and have been shown to be very effective for this purpose. The trade-off for these benefits, however, is typically an increase in computational costs and potential for overfitting.

Overall, the progression represented in Table~\ref{tab:eeg_feature_extraction_summary} demonstrates a distinct shift towards unified, or so-called multi-domain, deep representations, further emphasizing the significance of so-called joint spatial, temporal, spectral features in modern emotion recognition paradigms based on EEG technology.


\subsection{Feature Selection}
In emotion recognition datasets using electroencephalogram (EEG) signals, datasets contain high-dimensional feature vectors, including a high number of channels as well as different frequency band features. At the same time, several datasets contain a relatively low number of instances compared to the high dimensionality of the dataset. As a result, there is a great tendency for the occurrence of the curse of dimensionality, which may cause models to overfit as well as possibly ignore several relevant features in the dataset, which often reduces model efficiency as well as generalization of the classifier. Feature selection is one of the main concepts in pattern recognition as well as machine learning that helps reduce a high number of features in a dataset, containing only the most efficient as well as unique features, which can help in improving the efficiency of a classifier in pattern recognition as well as in improving the efficiency of the machines used~\cite{hamzah2024heliyon}.

\subsubsection{Filter Methods}
Filter-based feature selection is one of the most used feature selection techniques in EEG-based emotion recognition systems. This is mainly because of its relative ease of implementation, speed, and classifier-agnostic nature. Filter-based feature selection approaches attempt to measure the significance of the features before actually training a classifier. Hence, these are generally employed as a pre-processing step of the machine learning pipeline to reduce dimensionality and improve the learning efficiency of downstream classifiers.

The relevance of the filter method is determined by the nature of EEG data, which is usually characterized as being both high-dimensional and noisy in nature. There is usually an involvement of emotion-related features as well as other features in a pool of redundant and irrelevant information. Filtering out these less informative features at an early stage enables filter approaches to build better classification performance, reduce overfitting, and improve interpretability. This is particularly significant for EEG-based emotion recognition tasks.

There are many different filter-based methods for selecting good features. Two examples of filter selection methods are correlation-based selection and mutual information (MI) selection. A correlation-based method examines the correlation between each feature and the emotional label it is related to in order to find all of the features that have a high correlation with the individual emotional labels and the overall target emotion label. In an MI-based method, the MI score of each feature is calculated with respect to the associated target label as well as with respect to all other features, to assess the relevance and redundancy of each feature. Similarly, each feature in the mRMR framework is evaluated based on its MI to the target label as well as its MI compared to all other features. The purpose of the mRMR framework is to identify a small, non-redundant collection of features that provide the most information when identifying emotion labels. Extensions of the mRMR framework have been developed that incorporate methods such as sequential forward search or floating search, allowing for a larger, more exhaustive search of the feature space with improved efficiency.

Filter-based methods have certain inherent limitations; they may produce suboptimal subsets of features when there are emotional patterns throughout multiple feature combinations or only when comparing each feature separately and independently. Therefore, the subset of features determined by filter methods will not always correspond to an optimal decision boundary that a classifier might find in an EEG-based emotion recognition system, and so may lead to poor classification performance when classifiers use the features selected from the filter methods. However, filter methods remain viable techniques for EEG emotion recognition systems because of their large-scale ability to apply to general tasks and their ability to be generalized.

\subsubsection{Wrapper Methods}
Use of wrapper-based feature selection provides an effective method for determining which EEG features are the most informative for classifying emotions; however, the wrapper method is generally more computationally expensive than the filter method. Filter methods assess individual features based on their characteristics or quality, whereas wrappers utilize a learning algorithm when performing the selection process. Therefore, wrappers can assess how well various combinations of features perform during the actual classification process, providing a more tailored and often higher-performing set of features.

The utility of the wrapper method within the context of EEG emotion recognition is due in part to the fact that EEG features exhibit complex interactions and nonlinear relationships between them, which is especially prevalent in affective EEG data. Affective EEG data representations tend to display and reflect subtle emotional responses, which may occur simultaneously/ly across many different brain regions and frequency bands, making it necessary to identify interdependencies between EEG features. Given that the wrapper method assesses combinations of the EEG feature subset's performance as a whole, it allows the evaluation and optimization of all combinations of the EEG feature subset for performance on a specified classifier.

Multiple methods showcase the wrapper method. An established technique, Recursive Feature Elimination (RFE), works by sequentially eliminating some of the least effective features, based on how much they influence the classifier's weight or accuracy, thereby refining the size of the feature subset over a series of iterations. Another increasingly popular evolutionary technique, Genetic and Differential Evolution, is used to systematically explore the very large EEG feature space effectively. The feature candidates are continuously evolved from iteration to iteration by simulating biological functions, such as mutation, selection, and cross-over; the evolution process is monitored using the classifier's ability to validate each of those combinations for maximum performance. These methods ultimately produce very effective combinations of features that will contribute to the increased performance and generalizability of emotion recognition across different subjects.

Despite these advantages, the wrapper methods also have their challenges. Their requirement to train the classifiers multiple times as different feature subsets are examined makes the process computationally expensive; this is particularly true when the feature set is large and/or when a computationally intensive classifier is selected for training on the feature set. As such, wrapper methods may not be very useful for practical real-time or resource-constrained scenarios. Another challenge is the possibility of overfitting due to inadequate regularization of the evaluation metric for selecting subsets of features, or when the feature selection method is too closely bound to the specific dataset used for its selection. Nevertheless, wrapper methods continue to remain viable methods for identifying high-quality subsets of features that will enhance emotion recognition for EEG applications.


\renewcommand{\arraystretch}{1.2}
\setlength{\tabcolsep}{5pt}
\begin{small}
\begin{longtable}{
>{\raggedright\arraybackslash}p{0.13\textwidth}
>{\raggedright\arraybackslash}p{0.20\textwidth}
>{\raggedright\arraybackslash}p{0.12\textwidth}
>{\raggedright\arraybackslash}p{0.18\textwidth}
>{\raggedright\arraybackslash}p{0.18\textwidth}
>{\raggedright\arraybackslash}p{0.19\textwidth}
}
\caption{Representative studies on EEG feature selection techniques for emotion recognition.}
\label{tab:eeg_feature_selection_summary} \\

\toprule
\textbf{Study} & \textbf{Approach} & \textbf{Feature Selection Method} & \textbf{Advantages} & \textbf{Limitations} & \textbf{Key Findings} \\
\midrule
\endfirsthead

\multicolumn{6}{c}{\textit{Table~\ref{tab:eeg_feature_selection_summary} continued}} \\
\toprule
\textbf{Study} & \textbf{Approach} & \textbf{Feature Selection Method} & \textbf{Advantages} & \textbf{Limitations} & \textbf{Key Findings} \\
\midrule
\endhead

\midrule
\multicolumn{6}{r}{\textit{Continued on next page}} \\
\endfoot

\bottomrule
\endlastfoot

Xu \textit{et al.}~\cite{xu2021_pairwise_redundancy} & Orthogonal regression for pairwise redundancy reduction & Embedded & Removes overlapping features, boosts relevance & Assumes linearity in correlations & Improves generalization across individuals \\

Kannadasan \textit{et al.}~\cite{kannadasan2023_de_eeg} & Differential evolution for feature subset selection & Wrapper & Searches compact, accurate feature sets & Evolution search is slow at scale & Increases classification stability in DEAP \\

Liu \textit{et al.}~\cite{liu2018emdfeature} & EMD with optimal subset evaluation & Wrapper & Efficient combination of decomposition and selection & EMD results vary with input noise & Smaller feature sets yield higher accuracy \\

Wang \textit{et al.}~\cite{wang2019channel} & NMI-based EEG channel selection & Filter & Reduces redundancy and sensor count & Subset may vary across subjects & Preserves accuracy with fewer channels \\

Peng \textit{et al.}~\cite{peng2005mrmr} & Minimum Redundancy Maximum Relevance (mRMR) for EEG feature ranking & Filter & Selects maximally relevant yet minimally redundant features & Requires discretization for continuous signals & Improves robustness of EEG-based classifiers via mutual information criteria \\

Al-Nafjan \textit{et al.}~\cite{alnafjan2017feature} & ReliefF-based statistical feature ranking & Filter & Fast computation and noise-tolerant ranking & May overlook nonlinear inter-feature interactions & Demonstrates effective dimensionality reduction for valence classification \\

Jenke \textit{et al.}~\cite{jenke2014feature} & Comparative evaluation of filter-based feature ranking methods & Filter & Identifies most discriminative EEG descriptors & Dependent on classifier compatibility & Highlights mutual information and Fisher score as effective EEG selectors \\


Subasi~\cite{subasi2007genetic} & Genetic algorithm for optimal EEG feature subset & Wrapper & Explores global search space effectively & Computationally expensive for large feature pools & GA-based selection improves classification accuracy over manual selection \\

Guler \& Ubeyli~\cite{guler2005adaptive} & Adaptive neuro-fuzzy inference with feature subset search & Wrapper & Combines nonlinear modeling with subset optimization & Risk of overfitting with small datasets & Wrapper optimization enhances emotion-related EEG separability \\

Shen \textit{et al.}~\cite{shen2010feature} & Sequential forward selection with SVM evaluation & Wrapper & Produces compact feature subsets & Greedy search may miss global optimum & Reduces dimensionality while preserving classification performance \\


Guyon \textit{et al.}~\cite{guyon2002gene} & Recursive Feature Elimination (RFE) with SVM & Embedded & Integrates selection into classifier training & Sensitive to parameter settings & Embedded ranking improves generalization in high-dimensional data \\

Zhang \textit{et al.}~\cite{zhang2016lasso} & LASSO-based sparse feature learning & Embedded & Automatically enforces sparsity during training & Linear constraint limits nonlinear capture & Sparse modeling enhances cross-subject stability \\

Li \textit{et al.}~\cite{li2019embedded} & Deep neural network with attention-based embedded selection & Embedded & Learns task-relevant features automatically & Requires large training data & Embedded attention improves discriminative EEG feature learning \\

\end{longtable}
\end{small}

Table~\ref{tab:eeg_feature_selection_summary} summarizes representative feature selection strategies employed in EEG-based emotion recognition and highlights the evolution from conventional statistical ranking methods to model-integrated sparse learning approaches. Overall, the studies demonstrate that feature selection plays a crucial role in addressing the high dimensionality, redundancy, and subject variability inherent in EEG signals. The table shows that filter methods provide computationally efficient redundancy reduction and channel optimization, making them suitable for real-time or resource-constrained systems. Wrapper methods, although computationally heavier, generally achieve improved classification stability by directly optimizing feature subsets with respect to classifier performance. Embedded approaches integrate feature selection within the learning process itself, enabling simultaneous model training and dimensionality reduction, which is particularly beneficial for deep learning-based frameworks.

Across all categories, a consistent finding is that reducing irrelevant or overlapping EEG features enhances generalization, improves robustness across subjects, and lowers computational burden without sacrificing accuracy. The table therefore reflects a broader research trend toward compact, discriminative, and task-adaptive feature representations, which are essential for scalable and cross-subject emotion recognition systems.

\subsection{Challenges and Strategies in EEG Data Handling}
Single-modal EEG emotion recognition remains constrained by practical data-handling issues that directly influence generalization, reproducibility, and deployment. Two recurring bottlenecks are (i) redundant or weakly informative feature sets that inflate model capacity requirements and (ii) limited labeled samples that encourage overfitting and unstable training. Recent work, therefore, treats data handling as a co-design problem, where feature selection, augmentation, and generation are integrated into the learning pipeline rather than being optional post-processing steps.

\subsubsection{Feature Redundancy}
Feature redundancy arises when handcrafted descriptors (or even learned embeddings) contain overlapping information across channels, bands, and time windows, so that many dimensions contribute little beyond correlated noise. Redundancy is especially common when multiple feature families are stacked (e.g., entropy- and spectrum-derived statistics computed per channel) and when spatial neighborhoods yield near-duplicate summaries. A common mitigation strategy is explicit feature selection using filter, wrapper, and embedded schemes, which retains a compact subset while controlling inter-feature dependence.

Redundancy-aware selection increasingly moves beyond simple ranking toward objective functions that penalize pairwise overlap. For example, orthogonal-regression-based formulations incorporate redundancy into the optimization target to suppress mutually dependent predictors while maintaining discriminative utility. A representative direction improves flexible sparse orthogonal regression by adding pairwise redundancy terms into the loss so that the selected subset is simultaneously compact and decorrelated \cite{xu2021_pairwise_redundancy}. Complementary work frames selection as a population-based search problem, where differential evolution explores feature subsets under cross-subject constraints to reduce duplication while preserving affect-relevant variability \cite{kannadasan2023_de_eeg}. More recent orthogonal-regression pipelines further combine regression and feature weighting, encouraging sparse, high-utility feature sets that reduce the effective hypothesis space in small-data settings \cite{xu2023_fsor_weighting}.

\subsubsection{Limited Data Challenges}
EEG emotion datasets typically remain modest in subject count, session diversity, and label density, and labels are expensive because they require carefully controlled elicitation protocols and reliable affect annotation. When deep models are trained on such limited samples, they often latch onto subject-specific idiosyncrasies or session artifacts, which weakens cross-subject portability. Practical pipelines therefore adopt two complementary strategies: (i) augmenting available samples through label-preserving transformations and (ii) generating additional samples via learned generative models.

\paragraph{Augmenting Data Samples}
Data augmentation creates new training instances by transforming existing trials while maintaining their emotion labels. For EEG, effective augmentation typically respects signal structure (temporal continuity, channel topology, and latent source composition) rather than applying generic image-style perturbations.

Decomposition-driven augmentation is one established approach. Independent Component Analysis (ICA) can separate latent sources, enabling augmentation in component space and reconstruction back into sensor space; this allows recombination-based operations that preserve physiologically plausible structure while increasing trial diversity \cite{kang2019_ica_aug}. Likewise, the Empirical Mode Decomposition (EMD) technique may, through the process, derive the Intrinsic Mode Functions (IMFs), for which the selection process that preserves the high correlation with the waveforms may lead to the new samples \cite{singh2023_imf_aug}.

Augmentation also supports semi-supervised regimes where labeled data are scarce. A typical pattern injects weak/strong noise perturbations and uses interpolation between labeled and unlabeled examples to create mixed samples, then aligns the two domains (labeled vs. unlabeled) through domain-adversarial learning. Parameter-efficient domain-adaptation formulations operationalize this idea by treating interpolation as both augmentation and regularization, improving learning stability under low-label settings \cite{zhang2022_parse}.

\paragraph{Generating Synthetic Data}
Unlike augmentation, data generation aims to produce novel samples from learned distributions. Generative adversarial networks are widely in use today, and Wasserstein variants are favored due to their generally more stable training and improved mode coverage.

GANs, specifically Conditional Wasserstein GANs, are able to generate diverse feature representations in EEG features conditioned on class information. This serves to increase within-class variability based on the generated samples from classes of interest without having to create direct transformations of the trials \cite{luo2018_cwgan}. In multi-modal contexts, GAN-based augmentation can generate paired modalities, for example, jointly generating EEG along with another type of signal. This improves consistency across modalities when downstream fusion is used \cite{luo2019_gan_multimodal}.

More recent approaches extend beyond synthesis to a focus on generation through reconstruction, where a model hides parts of the real EEG and tries to regenerate them. This “mask-and-regenerate” strategy produces realistic variants anchored to true recordings, which can be especially helpful when fully synthetic waveforms risk deviating from physiological constraints \cite{zhang2022_ganser}. Finally, cross-category generation explicitly targets decision-boundary enrichment by creating samples that increase inter-class diversity and boundary coverage; diversity-aware and boundary-aware losses are introduced to avoid trivial interpolations and improve the usefulness of generated data for discriminative learning \cite{zhang2024_cccl}.

\section{Modeling and Evaluation Frameworks (Content yet to be humanized)}

EEG-based emotion recognition methods structure a classification pipeline that transforms multichannel brain dynamics into affect labels through preprocessing, representation learning, and decision modeling \cite{rahman2021eegreview}. Contemporary reviews emphasize that deep learning methods increasingly replace handcrafted feature stacks by learning discriminative spatial-temporal abstractions from minimally processed EEG while still requiring careful protocol design for valid performance claims \cite{li2018cnn,rahman2021eegreview}. Model families differ in inductive bias, because convolutional encoders prioritize local spatial-spectral regularities \cite{li2018cnn}, recurrent encoders prioritize sequential dependencies \cite{yang2018rnn}, and graph formulations prioritize sensor topology and inter-channel relations \cite{wang2022gcn}. Evaluation settings, therefore, define methodological meaning, since subject-dependent protocols favor personalization, whereas subject-independent protocols favor invariance and generalization across individuals \cite{craik2019deep}.

\subsection{Subject-Dependent Analysis}

Subject-dependent analysis trains and tests within the same participant, so the learning objective emphasizes intra-subject separability under stable acquisition conditions \cite{rahman2021eegreview}. CNN-based pipelines often encode EEG as channel-frequency maps or time-frequency tensors to exploit localized spatial patterns \cite{tripathi2017deapcnn}. Hierarchical convolutional designs often improve feature abstraction without explicit handcrafted selection \cite{li2018hcnn}, and region-aware convolutional designs often encode hemispheric structure to strengthen within-subject discrimination \cite{cui2020regionalasymcnn}. Recurrent encoders model temporal context by capturing dependencies across windows \cite{yang2018rnn}, while convolutional-recurrent hybrids often provide strong within-subject discrimination when trial structure remains consistent \cite{shen20204dcrnn}. This protocol, therefore, provides a practical benchmark for architectural exploration, yet it also risks optimistic estimates because learned representations can encode subject-specific idiosyncrasies that do not transfer across users \cite{rahman2021eegreview}.

Interpretability-oriented designs increasingly complement accuracy in subject-dependent settings by highlighting salient channels, segments, and frequency components that correlate with affective states \cite{qing2019_interpretable_eeg_emotion}. Channel-wise attention and self-attention mechanisms assign adaptive importance weights across electrodes and time, so these mechanisms support both performance gains and qualitative explanations of model focus \cite{tao2020channelselfatt,li2022efficientcnncl}. Graph attention and topology-aware networks further encode relational structure among electrodes, so these methods often characterize functional interactions rather than treating channels as independent feature streams \cite{zhong2022graph}. Graph neural formulations also integrate sensor topology and relational priors through message passing \cite{wang2022gcn}. Even in this favorable regime, robust reporting still requires rigorous cross-validation design and leakage control, because window overlap and stimulus structure can inflate within-subject metrics when partitioning rules remain weak \cite{craik2019deep}.

\subsection{Subject-Independent Analysis}

Subject-independent analysis trains on a source cohort and evaluates on unseen subjects, so domain shift across individuals becomes the dominant error source \cite{li2018emotion}. Transfer learning and cross-subject adaptation methods reduce this mismatch by learning subject-invariant features through feature alignment, parameter sharing, and regularization, while preserving emotion-relevant variation \cite{zhang2021cross,li2018emotion}. Plug-and-play and adversarial domain adaptation frameworks explicitly align latent distributions between source and target subjects using unlabeled target data, so these methods target invariance without requiring full target supervision \cite{zhao2021ppda}. Domain-adaptive neural formulations further refine alignment to preserve discriminability under shift \cite{bao2021tldann}. Latent-representation similarity objectives also constrain transfer by regularizing feature geometry between domains \cite{li2019lrs}. Contrastive objectives encourage subject-invariant embeddings by pulling together emotion-consistent samples while pushing apart emotion-inconsistent samples across subjects \cite{shen2022contrastive}, and cross-corpus evaluation settings reveal how these invariances behave under dataset-level mismatch \cite{rayatdoost2018crosscorpus}.

More recent subject-independent methods integrate topology-aware backbones with adaptation modules that manage both cross-subject and cross-dataset variability \cite{xu2023dagam}. Domain-adversarial graph attention models align relational representations across domains while maintaining spatial-temporal structure \cite{xu2023dagam,zhong2022graph}. Graph attention-based spatial-temporal learning further supports subject-independent decoding by encoding dynamic interactions among electrodes \cite{zhu2022gatstpl}. Multi-source adaptation methods treat each subject or dataset as a distinct source and learn transferable prototypes or shared subspaces \cite{li2023msfran}. Pairwise and prototypical learning frameworks mitigate negative transfer under heterogeneous sources \cite{zhou2023prpl}. Parameter-efficient adaptation formulations reduce adaptation overhead while preserving transfer performance \cite{zhang2022_parse}. Broader domain generalization theory motivates training objectives that optimize shift-robust representations rather than single-domain accuracy \cite{zhou2022dgsurvey}.

% \subsubsection{Domain Adaptation Models}
% \label{sec:domain_adaptation}

% A persistent obstacle to \emph{subject-independent} EEG emotion recognition is domain shift, i.e., the mismatch between the training distribution (source domain) and the deployment distribution (target domain). In affective EEG, a ``domain'' typically corresponds to a \emph{subject}, \emph{session/day}, or \emph{dataset/device montage}, and domain shift arises from strong inter-subject variability, nonstationarity, and acquisition differences. Domain adaptation (DA) addresses this challenge by learning representations and decision rules that remain discriminative for emotion labels while reducing sensitivity to domain-specific cues \cite{ganin2016dann,gretton2012kernel,sun2016deepcoral}.

% In practical EEG emotion systems, models are often trained on multi-subject datasets but must generalize to unseen users under leave-one-subject-out (LOSO) or cross-session protocols. However, EEG distributions can vary substantially across subjects and sessions even under the same task, causing performance drops when optimizing only the source empirical risk. Formally, with labeled source data $\mathcal{D}s={(x_i^s,y_i^s)}{i=1}^{n_s}$ and (often unlabeled) target data $\mathcal{D}t={x_j^t}{j=1}^{n_t}$, domain shift implies $P_s(X,Y)\neq P_t(X,Y)$, which may reflect covariate shift ($P(X)$), label shift ($P(Y)$), or conditional shift ($P(Y|X)$). DA mitigates these effects by incorporating an \emph{alignment} term into training so that features extracted from source and target become closer under a chosen discrepancy while retaining emotion-relevant structure \cite{wu2020tleeegbci,wan2021tleeegsurvey,li2022transferreview}.

% Most modern DA methods for EEG emotion recognition combine supervised learning on $\mathcal{D}s$ with an explicit feature alignment objective:
% \begin{equation}
% \min{\theta_f,\theta_c}\ \mathcal{L}{\mathrm{cls}}(\mathcal{D}s;\theta_f,\theta_c);+;\lambda,\mathcal{L}{\mathrm{align}}(\mathcal{D}s,\mathcal{D}t;\theta_f),
% \label{eq:da_generic}
% \end{equation}
% where $\theta_f$ parameterizes a feature extractor $f{\theta_f}(\cdot)$ that maps $x\mapsto h\in\mathbb{R}^d$, $\theta_c$ parameterizes the emotion classifier, and $\lambda$ balances discriminability vs.\ invariance. In \emph{adversarial DA}, alignment is achieved by fooling a domain discriminator $d{\theta_d}$ (implemented via a gradient reversal layer), yielding a min--max game:
% \begin{equation}
%   \min_{\theta_f,\theta_c}\max_{\theta_d}\ \mathcal{L}_{\text{cls}}-\lambda\,\mathbb{E}\big[\log d_{\theta_d}(\mathbf{h}_S)+\log(1-d_{\theta_d}(\mathbf{h}_T))\big], 
% \end{equation} 
% which encourages $\mathbf{h}_S$ and $\mathbf{h}_T$ to become indistinguishable while preserving class structure \cite{pyjeng2021lowdimen}. This objective reframes generalization as a constrained optimization problem, because the model must maintain discriminability while suppressing domain cues that correlate with subject identity \cite{lyanrz2018tradaboo,lzhang2020guidesub}. Practical implementations vary in alignment scheduling, because overly aggressive alignment collapses emotion structure and reduces accuracy \cite{dwuyxu2020transfer}.
% In EEG emotion recognition, Bao \textit{et al.}~\cite{bao2021tldann} proposed a two-level domain adaptation neural network that applies alignment at multiple representation levels to improve cross-subject generalization; empirically, such hierarchical alignment can improve robustness, but adversarial training may be sensitive to hyperparameters and can over-align when class-conditional structures differ.

% Beyond single-source settings, Chen \textit{et al.} introduced MS-MDA for \emph{cross-subject and cross-session} EEG emotion recognition, using a shared extractor for domain-invariant features plus \emph{multiple one-to-one adaptation branches} (each source paired with the target) to reduce negative transfer; they reported improvements on SEED/SEED-IV transfer protocols, while the approach increases architectural/training complexity and depends on stable normalization choices across domains \cite{chen2021msmda}. To reduce the adaptation cost, Zhao \textit{et al.} proposed a plug-and-play domain adaptation strategy that can adapt a trained model to a new subject with minimal modification, reporting gains in cross-subject emotion recognition; a limitation is that performance may degrade when target shifts are large or when pseudo-labels are noisy \cite{zhao2021ppda}. For parameter efficiency, Zhang \textit{et al.} proposed PARSE, a parameter-efficient DA model for EEG emotion recognition that aims to adapt across subjects with fewer tunable parameters; such efficiency is attractive for deployment, but careful tuning is still needed to avoid under/over-adaptation \cite{zhang2022_parse}. Multi-source DA has also progressed: Li \textit{et al.} proposed MS-FRAN to leverage multiple source domains with improved transferability, showing stronger cross-subject performance, but with added training overhead and sensitivity to heterogeneous source quality \cite{li2023msfran}.

% Graph-based encoders have been combined with DA to better respect EEG channel topology. For example, Xu \textit{et al.} proposed DAGAM, a domain-adversarial graph attention model, reporting improvements for subject-independent emotion recognition by jointly modeling spatial relations and domain invariance; limitations include computational cost and the need for stable graph construction across datasets/montages \cite{xu2023dagam}. Representation learning objectives that explicitly target invariance are also influential: Shen \textit{et al.} \cite{shen2022contrastive} learned \emph{subject-invariant} representations via contrastive learning for cross-subject EEG emotion recognition, often improving robustness without requiring a strong discriminator, but performance can depend on augmentation design and batch composition. For \emph{cross-dataset} settings, Imtiaz \& Khan studied unsupervised domain adaptation to enhance cross-dataset EEG emotion recognition and reported gains under dataset/device mismatch; remaining challenges include montage differences and residual conditional shift that cannot be solved by marginal alignment alone \cite{Imtiaz2025}. Earlier domain-alignment baselines for affective EEG, such as latent-representation similarity objectives, remain relevant as lightweight alternatives, but may underperform when shifts are highly nonlinear \cite{li2019lrs}. Related empirical work on cross-corpus/cross-dataset affect recognition further supports that domain mismatch is a primary error source, motivating stronger DA and domain generalization strategies \cite{rayatdoost2018crosscorpus,zhou2022dgsurvey,cimtay2020pretrainedcnn}.

% Overall, recent evidence suggests that (i) multi-source DA (e.g., MS-MDA, MS-FRAN) is often preferable when many training subjects are available because it reduces negative transfer by respecting per-source heterogeneity \cite{chen2021msmda,li2023msfran}; (ii) parameter-efficient DA (e.g., PARSE) is attractive for deployment constraints, especially when rapid calibration is needed \cite{zhang2022_parse}; (iii) graph-based adversarial DA (e.g., DAGAM) can be especially effective when spatial/channel relationships are crucial, but costs more to train and maintain \cite{xu2023dagam}; and (iv) for strong cross-dataset mismatch, unsupervised DA is helpful but may require additional design (e.g., montage handling or conditional alignment) beyond marginal feature alignment \cite{Imtiaz2025,zhao2021ppda}. In practice, the ``best'' choice depends on the transfer scenario: cross-subject within one dataset often benefits most from multi-source DA, whereas cross-dataset transfer typically demands a combination of robust representation learning and explicit adaptation to device/montage differences.

% \subsubsection{Transfer Learning Models}
% \label{sec:transfer_learning_models}

% \textbf{Transfer learning (TL)} in EEG emotion recognition refers to reusing knowledge learned from \emph{source} domains (typically other subjects, sessions, or datasets) to improve performance on a \emph{target} domain where labeled data are scarce, expensive, or unavailable. In practice, the target may be a previously unseen subject (cross-subject/LOSO), a later recording session from the same subject (cross-session), or a new dataset with different devices/stimuli (cross-dataset). TL frameworks therefore aim to reduce the well-known accuracy degradation caused by distribution mismatch and limited calibration, by starting from a pretrained model/representation and adapting it using a small amount of target information \cite{wu2022tcds_tl_bci_review,wan2021neucom_transferreview,li2022tcds_emotiontransfer_review}.

% Transfer learning is \emph{especially important} for EEG emotion recognition because subject-specific variability and nonstationary acquisition conditions lead to poor out-of-domain generalization under realistic subject-independent deployment. Recent surveys and systematic reviews consistently identify TL and DA-style methods as major contributors to cross-subject and cross-session generalizability in EEG affective tasks, highlighting that practical aBCI systems must trade off recognition performance against calibration burden \cite{apicella2024neucom_systematic,wang2024csur_researchprogress,li2022tcds_emotiontransfer_review}. Conceptually, TL methods span (i) \emph{pretraining and fine-tuning} (re-optimizing a subset of parameters with target data), (ii) \emph{feature/distribution alignment} (reducing source--target discrepancy), (iii) \emph{multi-source transfer} (leveraging multiple subjects as multiple sources while mitigating negative transfer), and (iv) \emph{few-shot/meta-learning} (learning to adapt quickly with few target samples) \cite{wu2022tcds_tl_bci_review,wan2021neucom_transferreview,li2022tcds_emotiontransfer_review}.

% Li et al.\ \cite{li2018iconip_dan} did cross-subject EEG emotion recognition by applying a Deep Adaptation Network (DAN) with multi-layer distribution alignment (MMD-style) on SEED/SEED-IV, reporting accuracy improvements of \textbf{4.62\%} (3-class) and \textbf{4.24\%} (4-class) over baseline methods; limitation: discrepancy minimization may under-align complex, class-conditional shifts and typically requires careful feature-layer selection. Luo et al.\ \cite{luo2018iconip_wganda} did adversarial transfer with a Wasserstein GAN (with gradient penalty) to map source/target into a common feature space and reported that their WGAN-based DA significantly outperformed prior DA baselines on SEED and DEAP; limitation: adversarial training is computationally heavier and can be sensitive to stability/hyperparameter choices. Cimtay and Ekmekcioglu \cite{cimtay2020sensors_pretrainedcnn} did cross-subject/cross-dataset recognition by leveraging pretrained CNN backbones on minimally processed raw EEG (windowing, normalization) and achieved mean cross-subject accuracies of \textbf{86.56\%} (2-class) and \textbf{78.34\%} (3-class) on SEED and \textbf{72.81\%} (2-class) on DEAP, while SEED$\rightarrow$DEAP cross-dataset testing yielded \textbf{58.1\%}; limitation: cross-dataset transfer remains substantially harder and the image-like reshaping/pretrained vision backbone may not fully match EEG-specific inductive structure. Bao et al.\ \cite{bao2021frontiers_tdann} did two-level transfer by combining MMD-based alignment with adversarial confusion in a TDANN and reported cross-day transfer accuracy of \textbf{74.93\%} on SEED and cross-subject average accuracy of \textbf{87.9\%} on SEED (also reporting strong joy-vs-others discrimination on their multi-session dataset); limitation: reliance on topology-map construction and multi-stage losses increases pipeline complexity and may limit portability across preprocessing conventions. Zhao et al.\ \cite{zhao2021aaai_ppda} did \emph{calibration-efficient} transfer via Plug-and-Play Domain Adaptation (PPDA) by explicitly separating subject-invariant emotion components from subject-private components and achieved mean LOSO accuracy of about \textbf{86.7\%} on SEED while shortening calibration to \textbf{within a minute}; limitation: still requires a short target calibration stream (unlabeled) and the reported accuracy can be slightly below stronger but less practical DA baselines in fully offline settings. Chen et al.\ \cite{chen2021frontiers_msmda} did multi-source transfer using MS-MDA with multi-branch one-to-one adaptation (each source paired with the target) and reported cross-subject accuracy of \textbf{89.63$\pm$6.79} on SEED (and \textbf{59.34$\pm$5.48} on SEED-IV), outperforming single-source baselines; limitation: multi-branch training/ensembling increases computational and implementation cost as the number of sources grows. Bhosale et al.\ \cite{bhosale2022bspc_calibrationfree_metalearning} did \emph{few-shot / calibration-minimal} transfer by framing subject-independent emotion recognition as few-shot adaptation without fine-tuning per user, and reported that with only \textbf{20} reference samples from unseen subjects their approach improved accuracy by \textbf{8.56\%} (valence) and \textbf{7.53\%} (arousal) over a supervised SOTA baseline; limitation: performance depends on episodic sampling strategies and on how representative the small support set is for the target subject. Li et al.\ \cite{li2022compbiomed_metatransfer_connectivity} did cross-subject transfer by combining connectivity features with a multi-scale residual network and meta-transfer learning, reporting average cross-subject accuracies of \textbf{71.29\%} (valence) and \textbf{71.92\%} (arousal) on DEAP and \textbf{87.05\%} on SEED (binary); limitation: connectivity estimation and multi-module training add preprocessing/model overhead and may be sensitive to noise/artifacts in functional coupling measures. Quan et al.\ \cite{quan2023bspc_multisource_transfer} did multi-source transfer with representation learning (MR-VAE) and explicit \emph{source-domain selection} (global/subdomain similarity) and reported \textbf{92.83\%} (SEED) and \textbf{79.30\%} (SEED-IV), improving substantially over non-transfer baselines; limitation: requires a small amount of labeled target data and can still face negative transfer when domain selection is imperfect under severe distribution shifts. Lu et al.\ \cite{lu2023frontiers_dffnet} did hybrid TL by integrating domain adaptation with few-shot fine-tuning in DFF-Net and reported \textbf{93.37\%} (SEED) and \textbf{82.32\%} (SEED-IV), with ablations showing fine-tuning alone outperformed DA alone (e.g., \textbf{88.09\%} vs.\ \textbf{71.32\%} on SEED); limitation: the method requires at least a small amount of target samples and was noted to lack real-time online processing capability. Imtiaz and Khan \cite{imtiaz2025compbiomed_uda_gptds} did cross-dataset transfer via unsupervised domain adaptation with gradual target-sample selection (GPTDS) and selective test-time augmentation (PC-TTA), reporting \textbf{67.44\%} (DEAP$\rightarrow$SEED) and \textbf{59.68\%} (SEED$\rightarrow$DEAP) with improvements of \textbf{7.09\%} and \textbf{6.07\%} over baselines and a \textbf{15$\times$} reduction in TTA compute; limitation: cross-dataset accuracies remain lower than within-dataset transfer and target pseudo-sample selection can be sensitive under extreme domain gaps. Wang et al.\ \cite{wang2025eswa_pesd} did cross-dataset transfer with a pretrained ViT encoder selected from emotionally sensitive subjects (PESD) plus Mixup, triplet loss, and a domain discriminator, reporting best accuracies of \textbf{93.14\%} (SEED), \textbf{83.18\%} (SEED-IV), \textbf{93.53\%} (DEAP), and \textbf{92.55\%} (FACED); limitation: dependence on large-scale pretraining and compound alignment losses may increase training complexity and the approach assumes that sensitivity-based source pretraining generalizes broadly.

% \begin{align}
% \min_{\theta}\;\; \mathcal{L}_{\textsc{ft}}
% &=\frac{1}{|\mathcal{D}_t^{\ell}|}\sum_{(x,y)\in\mathcal{D}_t^{\ell}} \ell\!\left(f_{\theta}(x),y\right)\;+\;\mu \|\theta-\theta_s\|_2^2, \label{eq:ft_reg} \\[2pt]
% \mathrm{MMD}^2(\mathcal{H}_s,\mathcal{H}_t)
% &=\Big\|\frac{1}{n_s}\sum_{i=1}^{n_s}\phi(h_i^s)\;-\;\frac{1}{n_t}\sum_{j=1}^{n_t}\phi(h_j^t)\Big\|_{\mathcal{H}}^2. \label{eq:mmd_tl}
% \end{align}
% In \eqref{eq:ft_reg}, $f_{\theta}$ is a target model (e.g., an encoder+classifier), $\mathcal{D}_t^{\ell}$ is a small labeled calibration set, $\ell(\cdot)$ is the classification loss (e.g., cross-entropy), and $\mu\|\theta-\theta_s\|_2^2$ regularizes the update toward pretrained parameters $\theta_s$ to prevent overfitting under scarce target labels (fine-tuning, as used explicitly or implicitly in hybrid TL designs \cite{lu2023frontiers_dffnet,quan2023bspc_multisource_transfer}). In \eqref{eq:mmd_tl}, $h^s,h^t$ are source/target hidden features, $\phi(\cdot)$ maps features into an RKHS $\mathcal{H}$, and minimizing MMD encourages feature-distribution alignment (a core mechanism in discrepancy-based transfer and multi-source DA pipelines) \cite{li2018iconip_dan,bao2021frontiers_tdann,chen2021frontiers_msmda}.

% Overall, \emph{hybrid strategies combining fine-tuning and alignment} tend to be accuracy-leading when a small amount of target data is available (e.g., DFF-Net and MR-VAE+selection pipelines) \cite{lu2023frontiers_dffnet,quan2023bspc_multisource_transfer}. When the priority is \emph{fast calibration and user experience}, plug-and-play or few-shot/meta-style transfer is often preferable because it reduces or removes per-subject retraining while remaining competitive in accuracy \cite{zhao2021aaai_ppda,bhosale2022bspc_calibrationfree_metalearning}. For \emph{multi-source subject pools}, explicit multi-branch adaptation and source weighting/selection are effective at limiting negative transfer and improving robustness \cite{chen2021frontiers_msmda,quan2023bspc_multisource_transfer}. Finally, for \emph{true cross-dataset shifts} (device/stimulus/demographic mismatch), task-specific pretraining plus carefully designed alignment and test-time strategies (e.g., PESD; GPTDS+PC-TTA) currently appear most promising, though they remain compute- and design-intensive and still trail within-dataset transfer in absolute accuracy \cite{wang2025eswa_pesd,imtiaz2025compbiomed_uda_gptds}.

Domain shift is a primary reason why EEG emotion recognition models that perform well in subject-dependent settings may fail under subject-independent evaluation. In affective EEG, a domain typically corresponds to a \emph{subject}, \emph{session/day}, or \emph{dataset/device montage}. Because EEG is nonstationary and highly individualized, both marginal feature distributions and class-conditional structures can drift across domains. Consequently, models trained only by empirical risk minimization on multi-subject source data can degrade substantially under leave-one-subject-out (LOSO), cross-session, or cross-dataset testing. A dominant family of solutions is \textbf{transfer learning}, which reuses representations learned from source domains and adapts them to target domains via alignment, adversarial invariance, prototype structure preservation, or target-only (source-free) updates \cite{wu2020tleeegbci,wan2021tleeegsurvey,li2022transferreview,wu2022tcds_tl_bci_review,wan2021neucom_transferreview,li2022tcds_emotiontransfer_review,wang2024csur_researchprogress}.

\subsubsection{Transfer Learning Framework}
\label{sec:transfer_learning_framework}

Transfer learning is crucial for EEG emotion recognition because collecting extensive labeled calibration data for every new user is impractical, and EEG distributions vary across subjects and sessions even under identical elicitation protocols. Surveys consistently emphasize that subject-independent performance depends on explicit mechanisms to mitigate covariate/representation shift and negative transfer, especially under cross-session and cross-dataset deployment constraints \cite{apicella2024neucom_systematic,wang2024csur_researchprogress,li2022tcds_emotiontransfer_review}. Transfer learning in this context includes (i) \emph{domain adaptation} (same task, shifted distributions), (ii) \emph{domain generalization} (generalize to unseen domains without target data), and (iii) \emph{fine-tuning} (calibration-efficient parameter adaptation), often combined in hybrid pipelines.

\paragraph{Domain Adaptation}
A persistent obstacle to \emph{subject-independent} EEG emotion recognition is domain shift, i.e., the mismatch between the training distribution (source domain) and the deployment distribution (target domain). In affective EEG, a ``domain'' typically corresponds to a \emph{subject}, \emph{session/day}, or \emph{dataset/device montage}, and domain shift arises from strong inter-subject variability, nonstationarity, and acquisition differences. Domain adaptation (DA) addresses this challenge by learning representations and decision rules that remain discriminative for emotion labels while reducing sensitivity to domain-specific cues \cite{ganin2016dann,gretton2012kernel,sun2016deepcoral}.

In practical EEG emotion systems, models are often trained on multi-subject datasets but must generalize to unseen users under leave-one-subject-out (LOSO) or cross-session protocols. However, EEG distributions can vary substantially across subjects and sessions even under the same task, causing performance drops when optimizing only the source empirical risk. Formally, with labeled source data $\mathcal{D}s={(x_i^s,y_i^s)}{i=1}^{n_s}$ and (often unlabeled) target data $\mathcal{D}t={x_j^t}{j=1}^{n_t}$, domain shift implies $P_s(X,Y)\neq P_t(X,Y)$, which may reflect covariate shift ($P(X)$), label shift ($P(Y)$), or conditional shift ($P(Y|X)$). DA mitigates these effects by incorporating an \emph{alignment} term into training so that features extracted from source and target become closer under a chosen discrepancy while retaining emotion-relevant structure \cite{wu2020tleeegbci,wan2021tleeegsurvey,li2022transferreview}.
Domain adaptation (DA) addresses $P_s(X,Y)\neq P_t(X,Y)$ by learning representations that remain emotion-discriminative while being domain-invariant. With labeled source data $\mathcal{D}_s=\{(x_i^s,y_i^s)\}_{i=1}^{n_s}$ and target data $\mathcal{D}_t=\{x_j^t\}_{j=1}^{n_t}$ (often unlabeled), many DA methods optimize:
\begin{equation}
\min_{\theta_f,\theta_c}\ \mathcal{L}_{\mathrm{cls}}(\mathcal{D}_s;\theta_f,\theta_c)\;+\;\lambda\,\mathcal{L}_{\mathrm{align}}(\mathcal{D}_s,\mathcal{D}_t;\theta_f),
\label{eq:da_generic_subjectind}
\end{equation}
where $f_{\theta_f}:x\mapsto h\in\mathbb{R}^d$ is a feature extractor, $\theta_c$ parameterizes the emotion classifier, $\mathcal{L}_{\mathrm{cls}}$ is supervised label loss on source, and $\mathcal{L}_{\mathrm{align}}$ reduces source--target feature discrepancy.

Beyond single-source settings, Chen \textit{et al.} introduced MS-MDA for \emph{cross-subject and cross-session} EEG emotion recognition, using a shared extractor for domain-invariant features plus \emph{multiple one-to-one adaptation branches} (each source paired with the target) to reduce negative transfer; they reported improvements on SEED/SEED-IV transfer protocols, while the approach increases architectural/training complexity and depends on stable normalization choices across domains \cite{chen2021msmda}. To reduce the adaptation cost, Zhao \textit{et al.} proposed a plug-and-play domain adaptation strategy that can adapt a trained model to a new subject with minimal modification, reporting gains in cross-subject emotion recognition; a limitation is that performance may degrade when target shifts are large or when pseudo-labels are noisy \cite{zhao2021ppda}. For parameter efficiency, Zhang \textit{et al.} proposed PARSE, a parameter-efficient DA model for EEG emotion recognition that aims to adapt across subjects with fewer tunable parameters; such efficiency is attractive for deployment, but careful tuning is still needed to avoid under/over-adaptation \cite{zhang2022_parse}. Multi-source DA has also progressed: Li \textit{et al.} proposed MS-FRAN to leverage multiple source domains with improved transferability, showing stronger cross-subject performance, but with added training overhead and sensitivity to heterogeneous source quality \cite{li2023msfran}.

Graph-based encoders have been combined with DA to better respect EEG channel topology. For example, Xu \textit{et al.} proposed DAGAM, a domain-adversarial graph attention model, reporting improvements for subject-independent emotion recognition by jointly modeling spatial relations and domain invariance; limitations include computational cost and the need for stable graph construction across datasets/montages \cite{xu2023dagam}. Representation learning objectives that explicitly target invariance are also influential: Shen \textit{et al.} \cite{shen2022contrastive} learned \emph{subject-invariant} representations via contrastive learning for cross-subject EEG emotion recognition, often improving robustness without requiring a strong discriminator, but performance can depend on augmentation design and batch composition. For \emph{cross-dataset} settings, Imtiaz \& Khan studied unsupervised domain adaptation to enhance cross-dataset EEG emotion recognition and reported gains under dataset/device mismatch; remaining challenges include montage differences and residual conditional shift that cannot be solved by marginal alignment alone \cite{Imtiaz2025}. Earlier domain-alignment baselines for affective EEG, such as latent-representation similarity objectives, remain relevant as lightweight alternatives, but may underperform when shifts are highly nonlinear \cite{li2019lrs}. Related empirical work on cross-corpus/cross-dataset affect recognition further supports that domain mismatch is a primary error source, motivating stronger DA and domain generalization strategies \cite{rayatdoost2018crosscorpus,zhou2022dgsurvey,cimtay2020pretrainedcnn}.

\subparagraph{Discrepancy-Based Methods}
Discrepancy-based domain adaptation methods aim to reduce the statistical divergence between source and target feature distributions in order to enable knowledge transfer across domains. In EEG-based emotion recognition, this approach is particularly relevant because physiological signals vary significantly across subjects, sessions, and recording environments, causing domain shifts that degrade model generalization. To address this issue, discrepancy-based methods explicitly measure and minimize distribution differences between domains using metrics such as Maximum Mean Discrepancy (MMD) and Correlation Alignment (CORAL). By aligning feature representations in a shared latent space, these techniques encourage the learned representations to become domain-invariant while preserving discriminative information for emotion classification.

Discrepancy-based DA minimizes explicit distribution distances (e.g., MMD/CORAL). Li \textit{et al.}~\cite{li2018iconip_dan} did cross-subject EEG emotion recognition by applying deep discrepancy alignment (DAN with multi-layer distribution matching) and reported accuracy improvements of \textbf{4.62\%} (3-class) and \textbf{4.24\%} (4-class) over baseline methods on SEED/SEED-IV; limitation: marginal discrepancy minimization may under-align nonlinear or class-conditional shifts and requires careful layer/feature selection. Bao \textit{et al.}~\cite{bao2021frontiers_tdann} did two-level DA by pairing discrepancy alignment (MMD) with adversarial confusion and normalization mechanisms, reporting strong cross-subject accuracy of \textbf{87.9\%} on SEED and cross-day transfer accuracy of \textbf{74.93\%} on SEED; limitation: multi-stage losses and topology mapping increase pipeline complexity and can be sensitive to scheduling. Chen \textit{et al.}~\cite{chen2021frontiers_msmda} did multi-source marginal distribution adaptation (MS-MDA) with per-source branches (one-to-one adaptation) and reported cross-subject accuracy of \textbf{89.63$\pm$6.79} on SEED (and \textbf{59.34$\pm$5.48} on SEED-IV), outperforming single-source baselines; limitation: computation and memory scale with the number of sources, and normalization stability across domains is required.

Overall, discrepancy-based domain adaptation methods attempt to improve cross-subject EEG emotion recognition by explicitly minimizing distribution mismatches between domains. Approaches such as deep adaptation networks, multi-level discrepancy alignment, and multi-source marginal distribution adaptation have demonstrated improved classification performance on benchmark datasets like SEED and SEED-IV. However, these methods still face several challenges, including sensitivity to feature-layer selection, increased computational complexity in multi-source settings, and difficulty in fully capturing nonlinear or class-conditional distribution shifts across domains.
\subparagraph{Adversarial-Based Methods}
Adversarial domain adaptation (DA) approaches aim to learn domain-invariant representations by introducing an adversarial training mechanism between a feature extractor and a domain discriminator. In EEG-based emotion recognition, adversarial methods are widely adopted to mitigate cross-subject and cross-session variability, which often causes significant domain shifts between training and testing data. These approaches draw inspiration from generative adversarial networks (GANs), where the feature extractor attempts to generate representations that confuse the domain discriminator, thereby encouraging the model to learn features that are indistinguishable across domains. By aligning feature distributions implicitly through adversarial learning, these methods can capture complex nonlinear domain relationships that may not be effectively handled by explicit discrepancy minimization techniques.

Adversarial DA uses a domain discriminator to enforce invariance. Jin \textit{et al.}~\cite{jin2017eegdan} did EEG emotion recognition by adversarial domain adaptation and reported improved LOSO performance compared with non-adaptive baselines; limitation: early single-source adversarial alignment can be sensitive to heterogeneous subjects and may not fully handle conditional mismatch. Luo \textit{et al.}~\cite{luo2018iconip_wganda} did adversarial transfer using a Wasserstein GAN objective (with gradient penalty) and showed improved stability and cross-subject performance on SEED/DEAP; limitation: adversarial training increases complexity and may require careful tuning to avoid instability. Zhao \textit{et al.}~\cite{zhao2021aaai_ppda} did calibration-efficient adversarial adaptation via plug-and-play DA by separating emotion-shared and subject-private components, achieving mean LOSO accuracy of about \textbf{86.7\%} on SEED while reducing calibration to \textbf{within a minute}; limitation: still assumes a short target calibration stream and can degrade under large target shifts or pseudo-label noise. Xu \textit{et al.}~\cite{xu2023dagam} did adversarial DA with a graph-attention encoder (DAGAM) to preserve channel topology while enforcing domain confusion, improving subject-independent recognition; limitation: graph construction and attention modules raise computation and sensitivity to montage mismatch. Yang \textit{et al.}~\cite{yang2024ssaamsda} did multi-source adaptation by aligning spectral--spatial attention patterns (attention-consistent alignment), improving robustness under cross-subject/cross-session transfer; limitation: attention alignment adds model complexity and may overfit under small targets.

\textbf{Domain Adversarial Networks (DANN)}
A canonical DANN objective combines label prediction with an adversarial domain confusion term:
\begin{align}
\min_{\theta_f,\theta_c}\max_{\theta_d}\;\; \mathcal{L}_{\mathrm{DANN}}
&=\underbrace{\frac{1}{n_s}\sum_{i=1}^{n_s}\ell\!\big(C_{\theta_c}(F_{\theta_f}(x_i^s)),y_i^s\big)}_{\mathcal{L}_{\mathrm{cls}}}
\;-\;\lambda\underbrace{\Big[\frac{1}{n_s}\sum_{i=1}^{n_s}\ell_d\!\big(D_{\theta_d}(F_{\theta_f}(x_i^s)),0\big)
+\frac{1}{n_t}\sum_{j=1}^{n_t}\ell_d\!\big(D_{\theta_d}(F_{\theta_f}(x_j^t)),1\big)\Big]}_{\mathcal{L}_{\mathrm{dom}}},
\label{eq:dann_subjectind}
\end{align}
where $F_{\theta_f}$ is a feature extractor ($h=F(x)\in\mathbb{R}^d$), $C_{\theta_c}$ is an emotion classifier, $D_{\theta_d}$ is a domain discriminator, and $\ell(\cdot),\ell_d(\cdot)$ are label and domain losses (often cross-entropy). The trade-off $\lambda$ controls how aggressively domain cues are suppressed (via gradient reversal) while emotion discriminability is maintained \cite{ganin2016dann,wu2022tcds_tl_bci_review}. In practice, strong DANN-style alignment is often paired with normalization/discrepancy losses to stabilize training and prevent class collapse, as in TDANN and related hybrids \cite{bao2021frontiers_tdann,luo2018iconip_wganda}.

Overall, adversarial domain adaptation techniques aim to learn domain-invariant EEG representations by training a feature extractor to confuse a domain discriminator while preserving emotion-discriminative features. Methods ranging from early adversarial alignment models to more advanced architectures incorporating Wasserstein objectives, graph-attention encoders, and multi-source attention alignment have demonstrated improved subject-independent emotion recognition performance. Despite these advances, adversarial approaches still face challenges such as training instability, increased computational complexity, sensitivity to domain heterogeneity, and reliance on calibration data or pseudo-label quality in certain scenarios.

\subparagraph{Prototype/Cluster-Based Methods}
Prototype or cluster-based domain adaptation methods aim to preserve class-discriminative structure while aligning source and target domains. Instead of only matching marginal distributions, these approaches typically construct class prototypes or cluster centers and align target samples using pseudo-labels or similarity measures. Such strategies are useful in EEG emotion recognition where class structure may shift across subjects, making purely distribution-based alignment insufficient.

Prototype/cluster-based DA preserves class structure via pseudo-labeling and prototype alignment. Peng \textit{et al.}~\cite{peng2022jfaglp} did class-aware DA by joint feature adaptation and graph adaptive label propagation for cross-subject emotion recognition, improving performance by leveraging graph-structured pseudo-label relations; limitation: label propagation can amplify early pseudo-label noise if target confidence is poor. Shen \textit{et al.}~\cite{shen2022contrastive} did subject-invariant representation learning using contrastive objectives for cross-subject emotion recognition, often improving robustness without requiring a strong discriminator; limitation: gains depend on augmentation policy, batch composition, and the stability of positive/negative pair construction.

Overall, prototype and cluster-based approaches aim to maintain class consistency during adaptation by using pseudo-labels, graph propagation, or contrastive clustering mechanisms. These strategies often complement discrepancy or adversarial alignment by explicitly preserving class structure, though their performance can be sensitive to pseudo-label quality and representation stability.

\subparagraph{Source-Free Adaptation}
Source-free domain adaptation (SFDA) addresses scenarios where the original source data are unavailable during adaptation, often due to privacy, storage, or data-sharing restrictions. Instead of accessing source samples, SFDA methods adapt a pretrained source model directly using only target-domain data, typically through pseudo-labeling, self-training, or test-time adaptation mechanisms.

Source-free DA adapts models using only the target data at adaptation time (often due to privacy or data sharing constraints). Imtiaz \& Khan~\cite{imtiaz2025compbiomed_uda_gptds} did cross-dataset unsupervised target-side adaptation using gradual pseudo-target selection (GPTDS) and selective test-time augmentation (PC-TTA), reporting \textbf{67.44\%} (DEAP$\rightarrow$SEED) and \textbf{59.68\%} (SEED$\rightarrow$DEAP) with improvements of \textbf{7.09\%} and \textbf{6.07\%} over baselines and a \textbf{15$\times$} reduction in test-time augmentation compute; limitation: cross-dataset accuracy remains substantially lower than within-dataset transfer and pseudo-sample selection can be brittle under extreme domain gaps.

Overall, source-free DA provides a practical solution when source datasets cannot be accessed during deployment. While approaches such as GPTDS demonstrate promising improvements for cross-dataset EEG emotion recognition, their performance still depends heavily on the reliability of pseudo-label selection and robustness to large domain discrepancies.

\paragraph{Domain Generalization}
Domain generalization (DG) targets invariance without using target-domain data during training. It treats subject identity as a nuisance factor and trains on multiple source domains to learn features that generalize to unseen subjects. In EEG emotion recognition, DG is closely related to multi-source training, invariance-promoting regularization, and self-supervised objectives that reduce reliance on subject-specific cues; broad DG principles motivate augmentation design, invariant representation learning, and robustness evaluation under cross-dataset shift \cite{zhou2022dgsurvey,apicella2024neucom_systematic}.

\paragraph{Fine-Tuning Strategies}
Fine-tuning strategies adapt pretrained models to a target domain using a small labeled calibration set. In EEG emotion recognition, fine-tuning is commonly used when limited labeled samples from a new subject or session are available. By updating only a subset of parameters or applying regularization during training, fine-tuning enables the model to retain previously learned knowledge while adapting to the specific characteristics of the target domain.

Fine-tuning adapts a pretrained encoder/classifier to the target with a small labeled calibration set. A regularized fine-tuning objective is:
\begin{equation}
\min_{\theta}\;\; \mathcal{L}_{\textsc{ft}}
=\frac{1}{|\mathcal{D}_t^{\ell}|}\sum_{(x,y)\in\mathcal{D}_t^{\ell}} \ell\!\left(f_{\theta}(x),y\right)\;+\;\mu \|\theta-\theta_s\|_2^2,
\label{eq:ft_subjectind}
\end{equation}
where $f_{\theta}$ is the adapted model, $\mathcal{D}_t^{\ell}$ is a small labeled target set, $\theta_s$ are pretrained parameters, and $\mu$ regularizes updates to avoid overfitting. Quan \textit{et al.}~\cite{quan2023bspc_multisource_transfer} did multi-source transfer with MR-VAE representation learning and explicit source-domain selection, reporting \textbf{92.83\%} (SEED) and \textbf{79.30\%} (SEED-IV); limitation: requires a small labeled target set and can still suffer negative transfer if domain selection is imperfect. Lu \textit{et al.}~\cite{lu2023frontiers_dffnet} did hybrid fine-tuning and DA in DFF-Net and reported \textbf{93.37\%} (SEED) and \textbf{82.32\%} (SEED-IV), with ablations showing fine-tuning alone outperformed DA alone on SEED; limitation: needs target samples and was noted to lack online real-time capability. Wang \textit{et al.}~\cite{wang2025eswa_pesd} did cross-dataset transfer by pretraining a ViT encoder (PESD) and adapting with Mixup, triplet loss, and a domain discriminator, achieving strong accuracies on multiple datasets (e.g., \textbf{93.14\%} on SEED and \textbf{93.53\%} on DEAP); limitation: large pretraining and compound losses increase training complexity and compute requirements.

Across subject-independent EEG emotion recognition, \textbf{multi-source DA} (e.g., MS-MDA and attention-aligned MSDA) is typically best when many source subjects are available because it reduces negative transfer by respecting per-source heterogeneity \cite{chen2021frontiers_msmda,yang2024ssaamsda}. \textbf{Discrepancy-based hybrids} (e.g., TDANN combining MMD and adversarial confusion) often offer strong within-dataset cross-subject accuracy but require careful tuning and can be complex \cite{bao2021frontiers_tdann}. \textbf{Calibration-efficient adversarial methods} (e.g., PPDA) are preferable when user burden is critical, trading some peak accuracy for rapid adaptation \cite{zhao2021aaai_ppda}. Under severe \textbf{cross-dataset} mismatch, source-free unsupervised adaptation and pretrained encoders are increasingly competitive, though absolute accuracy remains lower than within-dataset transfer and robustness hinges on pseudo-sample reliability and augmentation design \cite{imtiaz2025compbiomed_uda_gptds,wang2025eswa_pesd}. 

Overall, fine-tuning strategies remain an effective mechanism for adapting pretrained EEG emotion recognition models to new subjects or datasets when a small labeled calibration set is available. Recent works combining fine-tuning with multi-source transfer, domain adaptation, or large-scale pretraining have achieved strong cross-subject and cross-dataset performance. However, these approaches still depend on limited labeled target data and may introduce additional training complexity, especially when hybrid adaptation frameworks or large pretrained encoders are employed.

\subsection{Machine Learning and Deep Learning Methods}

Machine learning and deep learning methods define EEG emotion recognition models through three core design decisions: the representation that converts raw EEG into learnable inputs, the hypothesis class that maps representations to affect labels, and the optimization strategy that estimates parameters under limited and noisy supervision \cite{yroyhb2019deeplear}. The model family also implies an assumption about what remains stable across trials and subjects, because shallow models often assume engineered features already encode invariances \cite{smalar2019emotions}, whereas deep models often learn invariances from data through inductive bias and regularization \cite{wlizzh2021physiolo}. A practical description of ``the model'' therefore includes not only the classifier but also the feature extraction operator, the regularizers that control complexity, and the training protocol that prevents leakage-driven shortcuts in time-windowed EEG segmentation \cite{nssuha2020eegbased}. Model choice also reflects an implicit stance on nonstationarity, because feature-engineering pipelines often treat stationarity as a preprocessing outcome \cite{ztliuq2019electroe}, while end-to-end learners treat stationarity as a representation-learning target \cite{megger2019emotionr}. The model specification, therefore, remains inseparable from the data collection and annotation process, because stimulus design and subject instruction define the distribution that the optimizer actually sees \cite{ctanfs2018asurveyo}.

Machine learning and deep learning methods formalize EEG emotion recognition as supervised prediction from multichannel observations to affect labels, so the methodological pipeline couples signal representation with a discriminative decision rule \cite{smalar2019emotions}. A generic formulation defines an EEG segment as $\mathbf{X}\in\mathbb{R}^{C\times T}$ with $C$ channels and $T$ samples, and it defines a model $f_\theta(\cdot)$ that outputs a class posterior $\hat{\mathbf{p}}\in\Delta^{K-1}$ over $K$ affect classes. The learning objective commonly minimizes empirical risk with regularization,
\begin{equation}
\min_{\theta}\ \frac{1}{N}\sum_{i=1}^{N}\mathcal{L}\!\left(f_{\theta}(\mathbf{X}_i),y_i\right)+\lambda\lVert\theta\rVert_2^2,
\end{equation}
where $\mathcal{L}$ often uses cross-entropy $\mathcal{L}_{\text{CE}}=-\sum_{k=1}^{K}\mathbbm{1}[y=k]\log \hat{p}_k$. Review syntheses emphasize that methodological validity depends on protocol control, because windowing, overlap, and subject partitioning determine whether $\theta$ encodes emotion-relevant structure or leakage-driven shortcuts \cite{megger2019emotionr}. The same formulation also implies a modeling choice about label semantics, because discrete emotion categories, dimensional bins, and intensity scales define different decision boundaries even when the same architecture applies \cite{wlizzh2021physiolo,ctanfs2018asurveyo}. Label noise and class overlap, then shift the practical goal from ``perfect classification'' toward robust risk minimization under ambiguous supervision \cite{jwitte2020comparis}. Confidence calibration and error auditing, therefore, remain essential alongside accuracy metrics \cite{shkimh2021asemoaut,jcheng2021emotionr}. The training objective also interacts with sample construction, because heavy overlap in sliding windows increases dependence between training and test examples unless partitioning rules remain strict \cite{nssuha2020eegbased}.

Representation choice governs model capacity requirements, so shallow pipelines typically construct explicit feature vectors $\mathbf{z}=\phi(\mathbf{X})$ and train a classical classifier $g(\mathbf{z})$ \cite{sbwank2019ikknpred}, while deep pipelines learn $\phi_\theta(\cdot)$ jointly with $g_\theta(\cdot)$ \cite{yroyhb2019deeplear}. Time-frequency encoders often use short-time Fourier transforms (STFT) or wavelet-like decompositions, for example,
\begin{equation}
\mathrm{STFT}_{\mathbf{x}}(t,\omega)=\sum_{\tau}\mathbf{x}[\tau]\ w[\tau-t]\ e^{-j\omega\tau},
\end{equation}
and band-limited power features often use Welch-style spectra $P(f)$ and band energy $E_b=\int_{f\in b}P(f)\,df$. Deep models often bypass explicit feature design by learning convolutional, recurrent, attention, or graph operators that map $\mathbf{X}$ to latent embeddings $\mathbf{h}$ that preserve spatial topology and temporal context \cite{sliuxw2020subjecti}. This dichotomy also influences computational cost and deployment feasibility, because shallow methods shift effort toward offline feature engineering \cite{thzhou2020waveletb}, while deep methods shift effort toward training stability and hardware acceleration \cite{jcheng2021emotionr}. A practical system often mixes these paradigms by retaining signal transformations that stabilize nonstationarity and then applying learnable models on top \cite{cysain2018automate}. Artifact-aware automation further reduces brittleness under session drift \cite{sphadi2021automati}. Feature normalization and subject-wise standardization also matter because EEG amplitude scales differ across individuals and electrodes, and inconsistent scaling undermines both margin-based classifiers and gradient-based learners \cite{smalar2019emotions}.

Evaluation also shapes methodological emphasis, because subject-dependent settings reward personalization \cite{smalar2019emotions}, while subject-independent settings reward invariance under inter-subject shift \cite{vgupta2019crosssub}. A unified view defines a training distribution $P_S(\mathbf{X},y)$ and a target distribution $P_T(\mathbf{X},y)$ and measures generalization under $P_T\neq P_S$, so robust methods constrain representation drift and reduce discrepancy,
\begin{equation}
\mathcal{R}_T(f)\ \le\ \mathcal{R}_S(f)+\mathrm{disc}\!\left(P_S,P_T\right)+\epsilon,
\end{equation}
where $\mathrm{disc}(\cdot,\cdot)$ denotes a divergence proxy. This viewpoint motivates explicit shift-handling objectives and transfer strategies \cite{dwuyxu2020transfer}. Subject-specific covariates often dominate cross-subject error \cite{xjiang2019transfer}, while low-dimensional transfer formulations provide alternative routes to generalization \cite{pyjeng2021lowdimen}. Reporting practices matter because accuracy averages conceal instability across subjects, so protocol-aware analysis benefits from per-subject breakdowns and variance-aware summaries \cite{nssuha2020eegbased}. Cross-dataset evaluation further intensifies these concerns, because device differences and preprocessing heterogeneity amplify distribution shift beyond typical inter-subject variability \cite{lyanrz2018tradaboo}.

\subsubsection{Shallow Models}

Shallow models operationalize emotion recognition through an explicit feature map $\phi(\cdot)$, so they treat the classifier as a relatively simple decision rule whose success depends primarily on whether $\phi$ encodes emotion-relevant invariances \cite{ztliuq2019electroe}. The model description, therefore, centers on the feature family and selection strategy that control dimensionality \cite{plieta2019eegbased}, and it also specifies the classifier geometry that fits the resulting feature distribution \cite{gkpvee2019eegbased}. This paradigm supports interpretability because each dimension of $\mathbf{z}$ often corresponds to a physiological hypothesis that remains inspectable and comparable across subjects \cite{sphadi2019asurveyo}. Shallow pipelines also remain attractive under limited labels because compact features reduce sample complexity and stabilize estimation \cite{thzhou2020waveletb}.

Shallow models emphasize handcrafted descriptors and classical classifiers, so they typically decompose $\mathbf{X}$ into interpretable components and then learn decision boundaries in a feature space of moderate dimension \cite{plieta2019eegbased}. Decomposition-driven pipelines often use empirical mode decomposition (EMD) variants that represent each channel as a sum of intrinsic mode functions (IMFs),
\begin{equation}
x_c(t)=\sum_{m=1}^{M}\mathrm{IMF}_{c,m}(t)+r_c(t),
\end{equation}
and they compute statistics per IMF, per band, or per channel. Differential-entropy-style features represent band-limited EEG as approximately Gaussian with variance $\sigma_b^2$, so the differential entropy satisfies
\begin{equation}
h_b=\frac{1}{2}\log\!\left(2\pi e\,\sigma_b^2\right),
\end{equation}
which yields compact affect-sensitive descriptors under common preprocessing assumptions \cite{ztliuq2019electroe,gkpvee2019eegbased}. This feature-centric paradigm aligns with priors about band modulation and hemispheric asymmetry, so it facilitates hypothesis-driven analysis even when end-to-end learning remains feasible \cite{plieta2019eegbased}. Connectivity-style handcrafted representations complement bandpower features by encoding inter-channel coupling that relates to distributed affective processing \cite{tzhang2019spatialt}.

Artifact handling and feature selection remain central in shallow pipelines, because noisy channels and ocular or muscular contamination distort engineered descriptors \cite{cqlaih2018artifact}. Automated cleaning and preprocessing pipelines increasingly support repeatable feature extraction \cite{cysain2018automate}. ICA-based cleaning often models observations as $\mathbf{X}=\mathbf{A}\mathbf{S}$, estimates $\mathbf{W}\approx \mathbf{A}^{-1}$ such that $\mathbf{S}=\mathbf{W}\mathbf{X}$ yields statistically independent sources, and source rejection removes components that correlate with artifacts. Feature selection frequently optimizes a sparsity-constrained objective,
\begin{equation}
\min_{\mathbf{w}}\ \lVert \mathbf{y}-\mathbf{Z}\mathbf{w}\rVert_2^2+\alpha\lVert\mathbf{w}\rVert_1,
\end{equation}
where $\mathbf{Z}$ stacks features across samples and $\mathbf{w}$ encodes feature relevance. Selection also acts as an implicit robustness mechanism, because stable subsets of channels and bands reduce sensitivity to impedance variation and session drift \cite{sphadi2021automati}. Artifact removal and selection also interact, because residual artifacts inflate feature importance and bias the selected subset toward nuisance-driven dimensions \cite{cqlaih2018artifact,sphadi2021automati}. Practical shallow systems, therefore, treat cleaning, selection, and classification as a coupled design problem rather than independent modules \cite{ztliuq2019electroe}.

Classifier design in shallow settings prioritizes stability and interpretability \cite{sbwank2019ikknpred}. A linear SVM solves
\begin{equation}
\min_{\mathbf{w},b,\boldsymbol{\xi}}\ \frac{1}{2}\lVert \mathbf{w}\rVert_2^2 + C\sum_{i}\xi_i
\quad
\text{s.t.}\quad
y_i(\mathbf{w}^\top\mathbf{z}_i+b)\ge 1-\xi_i,\ \xi_i\ge 0,
\end{equation}
while $k$-NN predicts with neighborhood voting,
\begin{equation}
\hat{y}=\arg\max_{k}\sum_{i\in\mathcal{N}_k(\mathbf{z})}\mathbbm{1}[y_i=k].
\end{equation}
Ensemble strategies also appear because bagging and boosting reduce estimator variance when features remain noisy and redundant \cite{thzhou2020waveletb}. Calibration matters because confidence estimates guide downstream affect-aware actions, so miscalibrated margins can degrade decision reliability even when accuracy remains high \cite{jwitte2020comparis}. Shallow models, therefore, remain transparent baselines, yet they demand careful engineering to prevent fragile performance under subject and session variability \cite{megger2019emotionr}.

\subsubsection{Deep Learning Models}

Deep learning models define emotion recognition through learnable feature extraction, so the model description emphasizes architecture-induced inductive bias and training constraints that prevent memorization of subject identity \cite{yroyhb2019deeplear}. The architecture family determines which EEG structure becomes easiest to learn, because convolution promotes locality \cite{jcheng2021emotionr}, recurrence promotes sequential dependence \cite{sliuxw2020subjecti}, and topology-aware encoders promote relational coupling \cite{tzhang2019spatialt}. Training choices such as augmentation, normalization, and regularization then enforce invariance under nuisance variability \cite{saydn2020deeplear}. Deep models also amplify leakage and annotation bias when protocol discipline remains weak \cite{nssuha2020eegbased}.

Deep learning models parameterize $\phi_\theta(\mathbf{X})$ with high-capacity operators that learn hierarchical abstractions, so they reduce dependence on handcrafted descriptors and often improve robustness under moderate noise \cite{megger2019emotionr}. Canonical architectures include CNN-like encoders for spatial-spectral locality \cite{jcheng2021emotionr}, RNN-family models for sequential dynamics \cite{sliuxw2020subjecti}, and relational encoders that exploit inter-channel coupling \cite{tzhang2019spatialt}. A common deep formulation maps $\mathbf{X}$ to logits $\mathbf{o}\in\mathbb{R}^{K}$ and probabilities $\hat{\mathbf{p}}=\mathrm{softmax}(\mathbf{o})$, and it learns parameters by minimizing $\mathcal{L}_{\text{CE}}$ or a class-balanced variant. Deep methods shift emphasis toward regularization, because dropout, weight decay, early stopping, and augmentation influence whether the learned representation captures invariant affective structure \cite{shkimh2021asemoaut}. Augmentation remains particularly relevant for EEG because window boundaries, phase jitter, and residual artifacts create nuisance variability that supervised objectives otherwise absorb into spurious decision rules \cite{sphadi2021automati}. End-to-end learning, therefore, benefits from artifact-aware preprocessing and evaluation-time sanity checks, even when architectures claim robustness \cite{cqlaih2018artifact}.

Transformer-style attention increasingly appears because emotion-relevant cues exhibit long-range interactions across time and channels \cite{hheand2019channela}. Self-attention defines queries, keys, and values as $\mathbf{Q}=\mathbf{H}\mathbf{W}_Q$,  $\mathbf{K}=\mathbf{H}\mathbf{W}_K$, and $\mathbf{V}=\mathbf{H}\mathbf{W}_V$, and it computes
\begin{equation}
\mathrm{Attn}(\mathbf{Q},\mathbf{K},\mathbf{V})=\mathrm{softmax}\!\left(\frac{\mathbf{Q}\mathbf{K}^\top}{\sqrt{d}}\right)\mathbf{V},
\end{equation}
which yields data-adaptive aggregation over tokens that represent channels, time steps, or time-frequency patches. Attention mechanisms fit EEG decoding because relevance concentrates in subsets of electrodes and epochs, so learned weighting acts as a soft selection operator that complements convolution or recurrence \cite{sliuxw2020subjecti}. Attention-based designs also support post hoc analysis via saliency-like maps, yet interpretability remains conditional because weight patterns do not necessarily imply causal importance \cite{ctanfs2018asurveyo}. Deep model analysis, therefore, benefits from ablations and stability checks that separate physiological structure from optimization artifacts \cite{yroyhb2019deeplear}.

\paragraph{Standalone Models}

Standalone models define a single end-to-end mapping from EEG to emotion labels, so they serve as a clear instantiation of representation learning without architectural fusion confounds \cite{saydn2020deeplear}. The model specification therefore focuses on backbone type, input encoding, and the depth-width trade-off that determines expressivity versus overfitting risk \cite{jcheng2021emotionr}. Standalone designs remain important because they isolate the effect of inductive bias and training protocol, which supports reproducible benchmarking across datasets and evaluation splits \cite{megger2019emotionr}.

Standalone models employ a single deep backbone as the primary feature learner and classifier, so they provide a clean baseline for attributing gains to architecture rather than fusion. CNN-only pipelines often apply 2D or 3D convolutions over structured tensors that embed electrode layouts and time-frequency maps. A generic convolution layer computes
\begin{equation}
\mathbf{H}^{(l)}=\sigma\!\left(\mathbf{W}^{(l)} * \mathbf{H}^{(l-1)}+\mathbf{b}^{(l)}\right),
\end{equation}
where $*$ denotes convolution and $\sigma(\cdot)$ denotes a nonlinearity. Such models often achieve competitive accuracy under controlled protocols because they match the inductive structure of EEG maps, and they simplify training because they avoid multi-branch fusion instability \cite{sliuxw2020subjecti}. Standalone designs also facilitate fair benchmarking because a single backbone reduces confounding effects from heterogeneous preprocessing pipelines \cite{nssuha2020eegbased}. Lightweight variants align with real-time constraints, where latency and memory budgets constrain model size \cite{sbwank2019ikknpred}.

\paragraph{Attention-Enhanced Models}

Attention-enhanced models define EEG emotion recognition through content-adaptive weighting, so they treat relevance as a learnable function of the signal rather than as a fixed property of channels or time points \cite{hheand2019channela}. The model description therefore includes attention granularity, the scoring function that produces weights, and the integration rule that mixes attended features with the backbone representation \cite{jweijl2018learning}. These choices matter because EEG contains sparse emotion-relevant events embedded in broader background activity, so attention mechanisms increase signal-to-noise ratio by learning where and when discriminative evidence concentrates \cite{jxingk2018adaptive}.

Attention-enhanced models introduce adaptive reweighting across channels and time, so they prioritize salient electrodes and temporal segments while suppressing irrelevant activity. Channel-wise attention often learns a gating vector $\boldsymbol{\alpha}\in[0,1]^C$ and produces a reweighted signal $\tilde{\mathbf{X}}=\mathrm{diag}(\boldsymbol{\alpha})\mathbf{X}$, where
\begin{equation}
\boldsymbol{\alpha}=\sigma\!\left(\mathbf{W}_2\,\delta(\mathbf{W}_1\,\mathrm{pool}(\mathbf{X}))\right),
\end{equation}
and $\mathrm{pool}(\cdot)$ aggregates across time. Channel attention also acts as an implicit sensor-selection mechanism, which reduces sensitivity to noisy electrodes and facilitates reduced-montage systems \cite{jweijl2018learning,jxingk2018adaptive}. Temporal attention highlights salient segments within a trial, which matters when affect evolves gradually rather than instantaneously \cite{tzhang2019spatialt}. Attention-enhanced models also introduce optimization sensitivity, so they require careful learning rate schedules and regularization to avoid unstable convergence \cite{yroyhb2019deeplear}.

\paragraph{Hybrid Models}

Hybrid models define EEG emotion recognition through complementary composition, so they assume that no single operator captures all relevant structure across space, time, and frequency \cite{sliuxw2020subjecti}. The model description, therefore, specifies participating modules, the fusion stage, and coordination constraints that ensure comparable feature scales and stable optimization \cite{shkimh2021asemoaut}. These design choices matter because hybridization increases expressivity but introduces failure modes such as branch dominance and brittle dependence on specific preprocessing representations \cite{megger2019emotionr}.

Hybrid models combine complementary modules, so they aim to capture local spatial patterns and long-range temporal structure within the same architecture. CNN-RNN hybrids often pass convolutional embeddings $\mathbf{h}_t$ into gated recurrent units (GRUs) or LSTMs. A GRU update follows
\begin{equation}
\mathbf{z}_t=\sigma(\mathbf{W}_z\mathbf{h}_t+\mathbf{U}_z\mathbf{s}_{t-1}),\quad
\mathbf{r}_t=\sigma(\mathbf{W}_r\mathbf{h}_t+\mathbf{U}_r\mathbf{s}_{t-1}),
\end{equation}
\begin{equation}
\tilde{\mathbf{s}}_t=\tanh(\mathbf{W}\mathbf{h}_t+\mathbf{U}(\mathbf{r}_t\odot \mathbf{s}_{t-1})),\quad
\mathbf{s}_t=(1-\mathbf{z}_t)\odot \mathbf{s}_{t-1}+\mathbf{z}_t\odot \tilde{\mathbf{s}}_t,
\end{equation}
So the model integrates local feature extraction with sequential memory. This design supports multi-scale context because convolution captures short-term rhythms while recurrence captures longer temporal dependence that relates to sustained affect \cite{tzhang2019spatialt,hheand2019channela}. Hybridization supports modular ablation because each module contribution remains isolatable, yet the same modularity increases engineering complexity in synchronization and optimization \cite{nssuha2020eegbased}. Practical hybrids, therefore, benefit from disciplined reporting that separates architectural benefit from protocol artifacts \cite{wlizzh2021physiolo}.



\textbf{References}\\
1	((Journal articles)) a) A. B. Author 1, C. D. Author 2, Adv. Mater. 2006, 18, 1; b) A. Author 1, B. Author 2, Adv. Funct. Mater. 2006, 16, 1.\\
2	((Work accepted)) A. B. Author 1, C. D. Author 2, Macromol. Rapid Commun., DOI: 10.1002/marc.DOI.\\
3	((Books)) H. R. Allcock, Introduction to Materials Chemistry, Wiley, Hoboken, NJ, USA 2008.\\
4	((Edited books or proceedings volumes)) J. W. Grate, G. C. Frye, in Sensors Update, Vol. 2 (Eds: H. Baltes, W. Göpel, J. Hesse), Wiley-VCH, Weinheim, Germany 1996, Ch. 2.\\
5	((Presentation at a conference, proceeding not published)) Author, presented at Abbrev. Conf. Title, Location of Conference, Date of Conference ((Month, Year)).\\
6	((Thesis)) Author, Degree Thesis, University (location if not obvious), Month, Year.\\
7	((Patents)) a) A. B. Author 1, C. D. Author 2 (Company), Country Patent Number, Year; b) W. Lehmann, H. Rinke (Bayer AG) Ger. 838217, 1952.\\
8	((Website)) Author, Short description or title, URL, accessed: Month, Year.\\
9	…((Please include all authors, and do not use “et al.”))\\



% Please provide Biographies and photos for Essays, Feature Articles, Progress Reports, Reviews, and Perspectives for those authors who should be highlighted  
% These should be at most 100 words long
% For other article types this section can be removed
% Photographs should be 40mm broad and 50 mm high

\begin{figure}
  \includegraphics{pictures/bio-placeholder.jpg}
  \caption*{Biography}
\end{figure}

\begin{figure}
  \includegraphics{pictures/bio-placeholder.jpg}
  \caption*{Biography}
\end{figure}

\begin{figure}
  \includegraphics{pictures/bio-placeholder.jpg}
  \caption*{Biography}
\end{figure}

\begin{figure}
  \includegraphics{pictures/bio-placeholder.jpg}
  \caption*{Biography}
\end{figure}


% Table of contents entry should be 50 - 60 words long
% Image should be 55 mm broad and 50 mm high or 110 mm broad and 20 mm high


\begin{figure}
\textbf{Table of Contents}\\
\medskip
  \includegraphics{toc-image.png}
  \medskip
  \caption*{ToC Entry}
\end{figure}

\bibliographystyle{MSP}
\bibliography{references}
\end{document}
